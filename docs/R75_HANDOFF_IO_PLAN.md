# R75 医生交接、保存写入与验收计划

## 边界与当前证据

承接 R74 的 b2cb0c2 中文、窗口持久化和勤杂工队列修订。ce0894b 已上传、逐文件核验并推 GitHub；首轮实际运行结果为 NOT_PROVEN / NEEDS_INPUT，施工完成项不代表真机验收。

### 首轮设备结果与续测边界

- run `20260910-233002-fefd4aecc266`：正式60秒观察925帧、2775次World更新，模拟失败0；后续窗口压力完成两次私有存读档。玩家45份存档/配置及部署22文件返回后哈希不变。
- 当前私有繁忙输入载入成功，恢复41员工、13患者、14房间及 `humanoid_info` 窗口；慢窗口记录指向 `UIMachine`。测试把普通机器信息窗拒绝，尚未进入16/64KiB对照与后续容量。原Lua print未持久保存详细拒绝记录，所以完整首轮拒绝字段仍缺失。
- 限定放行真实原类、当前UI归属/注册、非暂停的UIMachine，让信息面板保持打开，不点击维修或替换。独立购买确认、未知窗口、派生类、错误归属仍停止；真实构造、native持久化/afterLoad及负例验证。
- NEEDS_INPUT使用已有native诊断通道；只读消费者明确区分有完整绑定、错误数0的主动停止与实际引擎失败。所有未完成容量项继续NOT_PROVEN。
- 续测复用首轮60秒样本和两次存读，不重复180秒窗口压力；缩短前置观察为1秒热身/1秒样本/1毫秒压力预算，保留完整30秒同档工作、4次ABBA写出及全部容量/自然服务时长。缩短前置观察不用于帧率结论。
- ce0894b远端ARM、媒体、协议通过；三个宿主入口的唯一失败均为版本测试仍要求R74。修正为当前R75并拒绝旧R74，版本测试与相关回归合验。
- 续测部署在写入前暴露工具约束冲突：要求每批必须包含程序，同时拒绝未改变文件，导致纯Lua差量无法发布。修为非空差量即可；程序有变化时仍最后发布，纯Lua差量不触碰原程序。新回归覆盖实际三文件发布、旧哈希干扰、丢失重命名响应、完整回滚及玩家/原程序不变。两次预检查停止记录保留，正式产品未因此变成混合版本。

正常业务链为：呼叫分配 → 步行抵达 → 房间接受员工并安排服务动作 → 呼叫完成。已复现旧代码在无法寻路时直接走完检查点、删除呼叫并触发空队列。R74 实玩三次医生警告是否全部属于该路径仍待现场验证。

### 第二轮设备证据与流程修订（09-11）

- run `20260911-000846-0babbfab3565`，候选 `5bb4908`，程序 SHA `ae95b5fbc39556fc6bbfdec6d8c298bd74a44b9c913b1e3ab347e4087de450bf`；45份玩家存档/配置前后不变。完整结果仍为 NOT_PROVEN / NEEDS_INPUT，模拟错误0，日志无截断/写入失败。GitHub该提交十项实际执行作业成功，正式最终封版门未执行。
- 当前繁忙私有输入：41员工、13患者、14房间。30.081秒观察283帧（约9.4FPS），841次World、281次小时更新；41人有成功更新，40人有动作覆盖。普通速度仍落后墙钟，不能宣称繁忙医院已流畅或触及硬件上限。
- 同一暂停帧的16/64/64/16KiB四次保存完全同字节：各417339字节，SHA `933c2a26a04be736af836a3a6eb1dff3c61e831abbc6e5c3b782ef5b81ac4112`。整体3230/2577/2635/2900ms，均值3065对2606ms；本批64KiB约快15%，增加48KiB短期缓冲。第四份已真实重载且状态指纹一致。最长单次fwrite为36.900/31.870/29.712/25.265ms。该批未复现旧10秒长写，保存长尾未根治；默认16KiB暂保留到合并判定，原始数据可独立复算。
- 连续性观察发生两名员工离开，原17人身份门保持NOT_PROVEN。固定continuity输入完整非执行解码确认仅1台接待台，owner为source15；source20无associated_desk。source15实际服务2名Patient，source20缺少空岗位，双人服务门缺少前提。保留旧门未完成，不伪造服务、不修改工资/经营规则。
- 修正测试编排：员工离开仍执行私有存读；后续繁忙验收独立从当前用户输入副本重新载入，真实招聘到目标数量，避免受历史小医院状态牵连。IO后恢复固定expanded起点，保持末关返回比较的同源语义。
- 第12关已经成功载入；首小时World:onTick调用GameUI:showBriefing。新守卫仅在第12关测试区间、实际level/difficulty一致、原UIInformation类/当前UI归属、文本严格等于当前语言level12说明且非暂停时保持窗口打开。没有点击确认、关闭或推进教学。其他信息/错误/购买窗口仍停止。真实构造、原生保存重载及负例已验证；是否匹配现场窗口等续测记录确认。
- 后续工作优先完成独立繁忙存读、末关往返与释放。岗位服务验收按“存在可用岗位和实际需求→接管→完成服务→动作续接”修订前提，历史恢复资格和双人证据不冒充已通过。性能研究下一步只限定41人实际场景的逻辑/绘制占比与保存长尾，不开启泛化重构。

保存链为：隔离临时文件 → 完成序列化 → 关闭文件 → 原子提交 → 恢复资源和运行状态。旧档在提交成功前保持有效。实玩保存耗时 2.749–12.426 秒，最慢 fwrite 累计 10.213 秒；当前粒度是 stdio 墙钟时间，不能拆成物理 SD 与文件系统耗时。

### 第三轮设备停点与音乐成功链修订（09-11）

- run `20260911-004155-24252eab0489`，候选 `493e25d`：结果FAIL，停止于接待观察期间的瞬时 `music is not actually playing` 检查；模拟错误0。45份玩家文件和4个本次发布文件回返哈希不变。该轮未到繁忙扩充与末关往返，不能沿用为通过。
- 原生循环先从SDL取出事件，再调用runtime_tick/benchmark，随后才分发music_over。曲目停止与下一首开始之间存在有效过渡期。DOCTOR本地已安装来源WAV时长100.71365秒；日志开始至停点附近103.23秒，量级吻合，但缺少当次精准曲终标记。旧FAIL保留，不能仅凭推测改成PASS。
- 对完整成功路径施工：音频回调给结束事件附播放代次；停止、重载与当前资源销毁失效该代次，已从队列取出的旧事件也无法推进新曲；每代只消费一次，队列拒收有明确失败状态。Lua测试只允许存在原生完成通知的短暂pending，1000ms内必须由同一Audio原结束回调成功续播、新代次确认后解除；暂停、手动Next冒充、load/play失败、丢事件与超时均拒绝。pending期间不执行保存或结束验收阶段，退出清理不用于补写成功。
- SDL_mixer完成回调兼用于解码终止，当前新增记录证明“结束通知后续播”；正常EOF与中途解码终止的细分仍未证明，不据此宣称所有音频故障已排除。1000ms是明确验收上限，新增gap_ms用于实测；测试不操作点唱机或修改曲目规则。
- 第三轮同帧ABBA四次保存均成功，整体3437/2685/2813/3215ms，A均3326ms、B均2749ms，约17.35%改善；第四份重载指纹一致。结合第二轮约15%改善，下一候选普通写出默认选64KiB，额外48KiB为短期普通堆使用，不占线性内存；保留16/64显式对照。原格式与跨版本读取、原子提交、短写/close/异常保护和峰值测试继续约束该选择。旧十秒长尾尚未复现与销项。

## 施工及销项顺序

### 集中收尾与第四轮证据（09-11）

- 用户要求一次完成余下收尾；不扩大双屏、键位、资源格式或经营规则。当前41员工、13患者、14房间私有输入继续使用，43人来自R73历史负载，R75容量入口按原40–45范围接纳，R74旧协议仍可复现。
- run `20260911-020500-2a7db5c28605`：安装产品983138d，程序SHA `38d58b800378e5f3595b28c4c78c00ace00740ac155dce1898d3213e29835ff9`；完整FAIL，模拟错误0，45玩家原件与8部署文件返回保护PASS。已提交15c35c4仅增加测试覆盖，其十项GitHub执行作业成功，最终封版门跳过。
- 本轮四份ABBA输出415678字节、SHA均为 `302e076773e02cd0576a155ccde90df10c653409bac9b9f8af2d78cd58602007`。整体3446/2694/2726/3014ms，16KiB均3230、64KiB均2710，改善16.1%；旧10秒长尾仍无复现。本轮41人运行120秒，无游戏模拟错误；旧招聘入口因资金/候选限制未达43，跳过了繁忙保存双窗口，现修入口而不改变医院。
- 第12关实际运行60.281840秒，762帧、2284World更新，私有存读后指纹不相同停止；末关返回/释放未完成。完整非执行解析保存余额70000.0，与固定上游第12关StartCash=70000整数对应。真实reference/candidate的string/FILE writer与两个reader共同证明数值表示差异会导致旧tostring误报。修订规范相等数值；大整数真实精度损失、余额/实体/tick/timer/action/工资/房间变化继续失败；所有比较调用方共享实现，失败有界输出首差异，诊断失败不覆盖原错误。电脑结果与设备唯一根因分开，续测仍须排除其他现场差异。
- 曲目DOCTOR至FORTUNE已经续播且未触发旧检查错误，未采到HANDOFF间隙诊断，不能填写已测换曲毫秒或正常EOF证明。旧医生三次错误是否都归属于不可达接管路径仍未直接证实。
- 本批完成受影响测试、源身份绑定、完整生成/ARM/native构建后，只差量部署并启动一次集中私有验收。验收先按实际阶段分别销项；固定17人旧档的自然离职/第二接待员缺岗位保持历史NOT_PROVEN，不用它们抹去独立41人容量证据。人工只在最终包补HOME、键盘取消和最新中文视觉，已验证且未变化的矩阵复用。

第五轮 `20260911-025011-abc45813682a`（8e36ffe）：45原件保护PASS、模拟错误0；FAIL发生于41人读档后的首次容量准入，尚未开始busy_1。运行循环可在新的世界更新/画面提交前再次调用验收；旧宿主step把墙钟、模拟和帧计数同时推进，遗漏此真实调度边界。修订准入为有界等待world/hour/entity/呈现各自完成且墙钟前进，120秒内没有完成量仍失败，不靠墙钟代替工作。回归按仅墙钟→世界→小时→实体→最后呈现逐项推进，并覆盖无工作超时；不会改游戏主循环或模拟速率。原失败证据保留，完成量条件和后续60秒观察保持。

同批完整结果预算检查：用第四轮实际字段长度补齐剩余阶段后，预计Lua摘要11849/12000字节，仅余151字节，第二接待窗口若完成还会增加字段。R75每次内存快照不再重复四个无对应测量的resource_*占位，完整原生/GPU资源观察仍在原load/LevelStable日志，读回继续保留native_gpu_release未证明。新增真实控制器最终汇总预算用四份实际尺寸IO字段、三段IO快照、长运行时钟及全部后续阶段共同检查128字段/12000字节；不扩大协议上限、不删失败/状态/内存余量或人员证据。59624bc的三文件候选已生成，因该同批检查发现而未装机，后续候选覆盖它。

| 顺序 | 工作 | 完成条件 |
|---|---|---|
| 1 | 房间交接契约修复 | 原版失败复现；正常接管、不可达、同房、拒绝、中断、旧动作及新归属测试通过 |
| 2 | 16/64 KiB 显式缓冲 | 两轮实测后候选默认64KiB；原格式字节一致；短写、关闭、清理和原子提交失败均保留保护 |
| 3 | 私有自动对照 | 同一暂停医院按 16/64/64/16 顺序写出；读回比较字节、完整耗时、最大写入停顿和内存；重载健康 |
| 4 | 动态设备保护 | 按当前所有存档及配置列举、读回、哈希备份；程序差量含新字体，二进制最后发布；旧事务明确审计 |
| 5 | 合并验证与构建 | 一次改变链路回归、完整原生目标与 ARM 目标构建、真实生成 Lua 初始化；来源与产物绑定 |
| 6 | 自动设备验收 | 私有繁忙医院、人员服务、连续存读、真实招募、末关往返、资源回收；读回明确逐项 PASS/FAIL/NOT_PROVEN |
| 6前置 | 音乐事件归属与异步验收 | 实际短WAV原生播放结束/旧事件拒绝/新曲续播、Lua成功及负例、全目标编译通过后合批，不重复媒体格式矩阵 |
| 7 | 保留有效结果 | 有实测收益才调整默认缓冲；保存中文 Git 提交和回退信息；计划按实际证据更新 |

本轮继续沿用上下屏和经营规则。音乐、语音不重复转换；已证明的中文/英文性能对比不重跑。系统键盘取消、物理 HOME、新中文视觉排版留给一次最短人工确认；既有键盘确认、合盖基本路径保留历史证据，不冒充新候选验收。

## 验收约束

- runner 使用私有输入副本、私有存档目录和独立配置，禁止覆盖玩家原件。
- 性能样本仅在模拟持续正常、无错误恢复失真时解释；30 FPS 不作硬性门槛。
- 局部宿主测试、完整构建、上传读回和实际游戏运行各自记录结果。
- 失败扩大到对应生命周期及所有调用方；不清理整个存档图、不用统一游荡动作掩盖房间错误。
- 重用固定上游、依赖缓存和已验证资源；测试集合按影响范围选择，全套测试未执行时明确记录。
