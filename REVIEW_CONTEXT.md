# Corsixh3ds：当前源码与上机准备

当前产品源码固定为 [c9375bf8556940d8f5a92cb4cde59bedef639d59](https://github.com/zzliu-coder/corsixh3ds/tree/c9375bf8556940d8f5a92cb4cde59bedef639d59)，tree `21468c3aa695a1113381cbe40a6f89f55caa460a`。请用这个固定链接审核。默认 main 仍是历史版本 844121c；本资料分支用于上下文，不代表对应目录里的历史代码就是最新产品。

本轮由原协调线程直接完成实现、验证和私人测试包，未再分派线程，也没有恢复轮询自动化。当前线程自检不称为独立上下文验收。最小可玩路径、Old 3DS 整机内存、帧率和物理生命周期仍为 **NOT_PROVEN**。

## 1. 目标与本轮取舍

目标保持：Old 3DS、无立体 3D，复用原游戏窗口。下一步在同一二进制上验证：

> 冷启动 → 菜单 → 第一关 → 建接待台和诊室 → 招聘 → 一名患者完成就诊 → 保存 → 退出重开 → 恢复。

初始版本曾在菜单前内存不足；保留的历史报告记载约 53.58 MiB 普通堆、约 52.6 MiB 已分配。此为旧版本记录，当前候选没有相同环境的设备测量。

工作集中在实际读取、持有、绘制和释放资源的路径。通用资源框架扩张、全套自绘界面、多线程流水线和 GPU 重写继续冻结。已有 E0 构建与验证成果保留，各项历史判定仍绑定原提交。

## 2. 本轮改变及成功路径

| 边界 | 已实现的成功路径 |
|---|---|
| Lua 与音库所有权 | 先准备新 Lua 环境及全部 native 表，再发布新音库；失败保留旧内容；回收旧 player 时按当前 mixer 所有者判断权限。 |
| 音效完成通知 | 3DS 路线统一在现有游戏线程排期和消费，事件携带值与令牌。停止、替换、暂停使旧事件失效；队列暂时拒绝时保留通知，下轮重试。 |
| 合盖与 HOME | 音频播放与完成通知共同冻结时间，恢复后继续剩余时长；原本由用户暂停的音效保持暂停；重复系统事件不重写原暂停状态。 |
| 实际精灵纹理 | 保留原编码精灵数据；正常与换色纹理共同使用固定元数据、6 MiB 计账上限的 FIFO 缓存。淘汰释放 SDL 纹理并清空真实 sprite 指针，需要时重新生成。 |
| 中间内存 | 临时 ARGB 像素与准备中的 SDL 纹理由局部所有者负责释放，包括异常路径。 |
| 读档保护 | 先保存当前进度到恢复槽，再读取请求文件。请求恢复槽本身时选另一个槽；保护存档失败时停止读档，避免覆盖目标。 |
| PCM 算术 | 32 位平台的容量预检使用足够宽的中间整数，拒绝溢出或超过既定预算的数据。 |

外部 R41 修复包的六组修改已整合到实际上游生成器与回归测试，随后补了通知调度、系统暂停和实际纹理驻留控制。产品修改为 `34f2071` 和 `42fa237`；`c9375bf` 补齐旧计时测试夹具所需的新接口，产品二进制字节不变。相对基线 a15a981 的[完整差异](https://github.com/zzliu-coder/corsixh3ds/compare/a15a981f31bdf9264e2a296a96019f85bd36ca92...c9375bf8556940d8f5a92cb4cde59bedef639d59)可直接查看。

## 3. 资源路线

原始目录部署与真实有界消费者一起使用：

- English 闭包仅保留 `english.lua`、`original_strings.lua`。
- 原版声音 DAT 已在电脑侧验证：747 个索引，其中 746 项可播放。此输入无需额外 RNC 解压，原始文件字节保持。
- 运行时按需读取/解码音效，PCM 与元数据上限 3 MiB；启动 PCM 0。
- 精灵保留原编码数据，SDL 纹理按需生成、容量受限。6 MiB 上限包含每纹理 512 字节预留；驱动与分配器真实开销仍以整机采样为准。
- 完整原版关卡保留，第一关是当前验收范围。音乐、片头和演示关闭；音效与广播开启。
- 下屏缩小显示完整 640×480 游戏，上屏原尺寸裁切中央 400×240。重要窗口可读性仍需设备操作验证。

原始目录的文件大小不能推出运行时峰值。主机对 VSPR 表的静态测量是 4,397 项、原编码驻留公式约 3.81 MiB；如果全部生成普通 RGBA 纹理，像素载荷约 14.54 MiB。新增缓存约束真实已生成纹理；此计算不代表设备实测节省量或整个进程余量。

## 4. 本轮验证边界

| 检查 | 当前证据 |
|---|---|
| 完整固定上游组装及最终 ARM 链接 | PASS；生成新 ELF 与 3DSX。8 MiB linear heap 强符号与 RuntimeSession 链接路径检查通过。实验资源路线的符号存在不代表 loose 路线强制挂载它。 |
| 四组 native C++ | GCC 13 Debug / Release、Clang Debug / ASan+UBSan 各 134 项通过；Debug 主测试重复 50 次通过。 |
| 统一 Python 回归 | PASS；195 项中 194 通过、1 项因 macOS 大小写不敏感文件系统跳过，0 失败、0 错误；保留原冻结基线 143 项。旧计时夹具补齐后，在 c9375bf 重新完成整套检查。 |
| 新音效及适配器检查 | 26 项通过；实际生成消费者、严格 Lua 5.4.8 API 检查、真实 SDL2、可控 mixer 行为；包括分配失败、GC、队列、计时、合盖及读档槽位。 |
| 真实音库 | ASan+UBSan 下 747 索引 / 746 播放、100 次释放/替换；缓存计账峰值 3,105,928 字节，受控临时分配释放完毕。主机 dummy audio，无物理音频输出验收。 |
| 实际纹理缓存 | 真实 SDL 软件渲染器连续生成/淘汰 5,000 个纹理；受控字节不超过预算，最终归零；已提交绘制的像素读回正确。 |
| 实际保存引擎 | 原 C++ serializer + 组装后的 persistance.lua + 平台适配器：7 组受控 table-world 场景通过，含损坏主文件/备份、失败清理、恢复槽、30 次保存读取。未运行真实医院 Map/native graphics。 |
| 私人 SD ZIP | PASS；801 个文件、163,925,078 字节；压缩 CRC 与全部文件 SHA-256 读回匹配。411 个原版游戏文件与只读输入逐一核对；旧回退包保留。ZIP 未公开上传。 |
| 设备及独立上下文审查 | NOT_PROVEN / 本轮未执行；本轮自检由同一实施线程完成。 |

macOS 留存 GCC 没有 libasan，故该编译器的 sanitizer 链接未成立；本轮以现成 Clang 执行 ASan+UBSan。macOS LeakSanitizer 未执行。正常错误输入场景会输出预期错误信息，其通过条件是正确拒绝、状态保持及清理结果。

## 5. 固定产物

3DSX 为 2,646,672 字节，SHA-256：

`d9ee5d59852970ab70352182cd2be85cfcee8c8291602cf01ee442731fd692e1`

ELF SHA-256：

`903bd8c09f41302402f44056ffe1b91e0b5f1dfe68b788e3b573f560e1e8e2e7`

固定上游 `56bd5d00f76331c7f76d7b696726a7926303ca0c`。最终设备运行入口为 `/3ds/corsixth/CorsixTH-3DS.3dsx`，对应 Lua、配置和原始游戏目录必须一起复制，首次验证须移走旧面板切换标记。

[本轮安全摘要](docs/review/R42-device-preparation-summary.json)记录同一候选的主机、最终 ARM 和私人包校验结果。它是本线程施工自检摘要，公网 CI、本轮独立审查与实际设备运行结果分别记录，不互相替代。

## 6. 后续审核与设备反馈

优先审实际生成文件：`th_lua_sound.cpp`、`th_sound.cpp`、`th_gfx_sdl.cpp`、`sdl_core.cpp`，以及 `runtime_3ds.cpp`、`platform.lua`、`persistance.lua`。源模板入口为 `tools/sound_lifetime.py`、`sound_callbacks.py`、`sprite_residency.py`、`load_recovery.py` 与 `integrate_corsixth.py`。

下一项决定工程方向的输入是同一候选的真实设备日志：菜单/第一关到达时间、整机内存低水位、完整帧统计、保存重开、HOME/合盖。持续运行、后续关卡和大型医院的边界随后验证。保留的 `RH07_PRODUCT`、`RH09_PRODUCT` 原 FAIL 不由本轮主机结果自动改写。

旧 R41 上下文可在[前一资料提交](https://github.com/zzliu-coder/corsixh3ds/blob/aa079ad4cd9d54b4e29d14db5b95907f7cf0483e/REVIEW_CONTEXT.md)查看。仓库中的旧 PUBLICATION_MANIFEST、旧 EXTERNAL_AUDIT_STATUS 及历史报告仍描述各自冻结版本。阅读资料无需重下大工具包；本轮复用原工具链及依赖，明确记录的一份新增源代码下载为约 366 KiB Lua，用于严格 API 检查；未重新下载完整工具输入包。

公开内容限源码、上下文与安全摘要。原版素材、原始音库、存档、私人 SD ZIP 和未脱敏本机日志均不上传 GitHub。
