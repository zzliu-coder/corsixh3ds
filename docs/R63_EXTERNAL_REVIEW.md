# R63 外部审查入口：R62 故障与性能续建

本入口现包含 R63 修复与独立重审处置。设备此前安装 R62；R63 设备效果仍为 NOT_PROVEN。后续以 [滚动主计划的执行面板](R63_PERFORMANCE_PLAN.md) 销项。

## 请固定审查身份

- 外审对象 `ecc6b6f8f7c863c19e4c73e25a180f5eeb86d71d` 的产品代码为 R62；R63 第一批为 `46f51ef`，本入口所在后续提交还加入了外审增补修复。请从当前 Git 提交读取固定身份，三者不可混用。
- 固定 CorsixTH 上游 `56bd5d00f76331c7f76d7b696726a7926303ca0c`，v0.70.1。
- 先读 [五框架施工表](R63_PERFORMANCE_PLAN.md)，再检查 `tools/integration/` 中生成真实上游的变换、`lua/3ds/` 适配器、`src/3ds/runtime/` 与 `include/cth3ds/`。
- 生成后的上游不作为另一份手改真源；测试夹具与生成工具可在电脑核验。

## 最短必要历史

独立重审已指出并在本机复现两项反例：32位文字哈希下有限宽度与 INT_MAX 碰撞导致错误命中；16名员工每轮固定顺序使模16采样偏向同一人。本轮分别增加请求宽度键、按轮次轮换采样。内层 Lua 错误在展开前保留堆栈，成功模拟完成量与派发分账。Linux CI 失败也纳入修复，无放宽警告或扩大 skip。

旧 Slot1 日志另含回调/空行动队列警告；资格不明时拒绝自动改写。原档只读，恢复默认保存独立前缀，恢复检查和健康基准独立报告。测试使用实际上游 errorHandler/Entity 片段与真实原生 serializer，并验证后续计时回调继续；私人旧档的实际恢复仍需设备结果。

早期软件绘制仅约 2～3 次/秒。GPU ordered-canvas 后端已进入真实游戏，用户反馈有明确流畅改善。当前仍在压缩 CPU 世界更新、存读档峰值和窗口长停顿。已经经历 GPU 图集坐标／队列生命周期花屏问题，相关修复和回归不能在下一次加速中丢失。

中文界面、所选播报语音和流式 PCM 音乐已接入，它们必须作为常态负载；用户优先运行性能，素材磁盘体积次要。保持现有 640×480 画布、上屏清晰局部、下屏触控总览、摇杆白框／边缘滚图。经营规则、上游速度档和随机行为需要保持。

R61 历史固定 Normal 样本约 11.7～11.9 FPS，世界／UI 等逻辑约 56ms，绘制约 23ms。总余量估算不能证明大块连续分配安全。目标 30 FPS+ 尚未证明，所有大关卡容量也未证明。

R62 已实现有界背景分块、弱源图查找＋近期集、3MiB 紧凑整页 LRU 图集、员工动态文字按变化发布、温度正常路径分支减少、输入表复用、序列化器定点 O2、扩建医院自动基准。电脑测试及 ARM 构建通过；本次真实游玩发现新增员工采样接入错误，暴露测试的严格模块环境覆盖不足。

## 两次实际失败的共同证据

程序 SHA-256 `b6515fdf8cb348084e65554119d23fec51db7fa23d21bf8a01fc5586e8893de4` 与安装记录一致。两个 gamelog 的相同最小摘录：

```text
SDL renderer: citro2d-ordered-canvas
Lua 5.4 run with api version: 2717, game version: v0.70.1, savegame version: 254
Error in timer handler:
sdmc:/3ds/corsixth/Lua/entities/humanoids/staff.lua:127: use of undeclared variable 'TH3DS'
stack traceback:
    [C]: in function 'th3ds.cpu_profile'
    sdmc:/3ds/corsixth/Lua/app.lua:1388: in function <sdmc:/3ds/corsixth/Lua/app.lua:1381>
    (...tail calls...)
Recovering from error in timer handler...
```

用户恢复后存到 Slot1；再次加载多数原有人物不动，新急诊患者可以移动。上游 `App:errorHandler` 的确认恢复闭包会将当前出错实体 `ticks=false`，该字段可以写进存档。当前仅证明这条调用链与现象一致；副本中具体受影响人数仍在核验。底部大窗口预加载也引用了未绑定的 TH3DS，应同时处理。

原始日志和私人存档已本地保留；本入口不上传原版美术、声音或存档。读取程序、日志及存档的完成不代表运行验收。自动 benchmark 已失败，停止实体后的高 FPS 样本不具备性能对照意义。

## 希望审查回答

1. 核实故障因果、严格变量环境覆盖和安全的副本恢复范围；避免无证据地开启所有 `ticks=false` 实体。
2. 按 A 少做工作、B 必要工作更便宜、C 少持有少搬运、D 时序调度、E 冷路径／长停顿五框架，找真实调用链里的根本成本与明确修改边界。
3. 优先审查员工／邻域、文字缓存碰撞、图集工作集冲突、完整存档副本与整数编码分配、模拟吞吐和错误采样隔离。检查会改变规则、绘制顺序或随机状态的隐藏副作用。
4. 对每项建议给出源码路径、复现／比较方法、最小实现、风险、收益证据等级和验收条件。清楚区分源码推断、主机实测、真机实测。
5. 质疑现有方案中不必要的复杂性或遗漏。不要仅复述本入口；发现明确反例请直接指出。源码审核无需在网页替用户运行受限制的访问、扫描或硬件操作。

请将已知 R62 回归与新发现分开。当前会继续施工，审查依据固定提交，后续修复通过独立中文提交更新。
