# R68起：架构重构工单与验收边界

2026-09-10。排序与销项唯一入口为[R63起五框架滚动主计划](R63_PERFORMANCE_PLAN.md)。本文件是其AR1–AR6的施工规格。当前已装机为R72，73轮窗口、10次私有存读档与原件保护完成；AR3的装配所有者已在R69完成，Staff/World最终源码迁移已在R70完成。AR5刷新门已通过high施工及独立复核、主线程精确合入和最终整目标/行为检查，完整生成及ARM与R73合批；AR6首个实验资源构建边界已有完整消费者与最终ELF驻留证据，现由high私有施工。AR4及AR6开发编排其余部分继续排队。下方R68/R69各阶段记录保留原时点；最新身份与验收结论以主计划页首为准，避免历史待办被重复施工。

### R73-AR5本地检查点（09-10）

- FrameScheduler缩为PanelRefreshGate，保留Panel首次/周期/提前请求/dirty/按笔/失败与恢复reset顺序；Game短路旧门。删除三种没有消费者的模拟/上屏/丢时决策，真实SimulationClock与PresentationClock字节不变。
- 主线程按精确15路径patch合入，保持R72日志、年度压力和显示标识。新common头/源、CMake、simulator及所有测试提取消费者一起迁移；首次整宿主目标、7TU平台syntax、native与simulator两项ctest通过5.750秒。
- 最终Runtime实际方法配合真实BottomUiController/SoftwareCanvas通过46组旧行为及像素对照，两个Python测试3.198秒；与高强度独立复核的私有20项native及生成路径证据分开记录。未修改的媒体、Lua初始化及其它恢复矩阵按身份复用。
- 旧SDL_UpdateWindowSurface失败仍清dirty、uint64极端回绕仍沿旧模型，未扩大本次改动。结构状态10字段收为4字段，不直接登记FPS收益或实际ARM对象节省。
- 装机仍R72，完整生成视图、host发现清单同步、最终ARM/链接与设备留给R73合批。小中文提交提供回滚边界，不触发单独上机。

### R69收口：装配所有者贯通，剩余迁移保留

- 完整生成737文件重复一致、305 Lua语法、7个真实生成模块、最终宿主全目标/ARM与七份调试产物通过；同一生成视图经owner进入打包。没有另建一条部署专用装配链。
- 完整327项原full-01为320通过/1失败/5错误/1既有skip，用时1267.694秒；六个问题属于两个测试提取消费者。修正新phase类声明和完整方法边界后，两个完整模块11个原ID通过；其余472路径与冻结产品字节/模式一致。复用316项，形成326通过/1skip覆盖闭环，原FAIL保留，无重编译或第二次全套。
- 唯一run `20260910-011901-2171b478de7b`完成并返回runner，11轮窗口/2次私有保存重载/27原件保护通过，严格完整性能窗通过。测试修正提交`d02efb8`与后续文档提交不冒充已装机的d0eb969产品。
- 真实缺口现在集中在Staff/World最终源码及其消费者、窗口冷加载和观测输出长停顿。按主计划在改动前梳理完整调用/生成/结果链，high承担重构与契约复核；纯字节等价迁移复用现有设备行为证据，与下一实际性能改动合批验机。
- 当前14.937FPS仅为本次真实观测，精确同态A/B与净收益、设备故障注入、完整旧档未来行为和大关/长时仍NOT_PROVEN。结构变清晰不直接登记帧率提升。

## 1. 判断与范围

审查的核心判断成立：产品行为隐藏在分阶段字符串替换、App多层方法包装和两个过宽的控制类中。先恢复明确的操作结果、状态归属和装配边界，再迁移源码与清理旧职责。按文件行数机械拆分，无法关闭这些问题。

审查冻结`a0427ce8738cfa4fce641973016b45ab67cdd40f`；R67装机`ee0dd57f83832c5e3c14a834bb3c365f48ef16fa`只增加测试夹具修正。主线程已核实此diff，并检查实际装配入口、Staff变换、FrameScheduler消费者和loose资源初始化条件。GPT-6 Astra high子流程独立复核真实Platform/C++契约及宿主故障注入。

初始high复核进一步确认`showError`自身的诊断异常也可覆盖主保存错误；attach失败还可留下底栏visible变更。9个真实Lua模块受控夹具重现这些原有行为，属于缺陷复现证据。随后固定A快照的17场景实际attach/Benchmark.new复现了半安装、失败包装叠层、继承外来adapter等问题；C按该原件逐项修复，宿主关闭结果见下方C检查点。

审查输入SHA-256：

- `CorsixTH-代码优化审查.md`：`8dd3b08a03d671076a4a4cdef284bdee79514165a13a32eb6592bd71a21c7f7e`
- `CorsixTH-审查证据.json`：`156309071df4417a56e0b5e20e0766a77afcc8f4f4f16f71f242129999094757`

附件属于审查证据；施工决定以核实后的接口、测试与主计划为准。宿主复现可以证明契约有缺陷，不能写成真机已发生该故障。

### 必须补充的四条边界

1. 当前C++把`handleAction`的false/nil升级为异常。动作重构须同时定义非致命no-op，保留菜单无世界、合法等待、软键盘取消、禁止平移等正常场景；不能直接把所有缺少目标的动作改为false。
2. 诊断不得改写保存提交事实。事务清理、`end_critical_io`和资源save-end具有真实职责，其失败要明确单列；不能作为可忽略日志一并吞掉。真实writer错误优先保留。
3. 当前loose已经条件跳过RuntimeSession挂载。隔离实验系统会减少理解与构建范围；普通内存与帧耗时收益需要测量，不能把整个实验资源预算当作可回收内存。
4. 完整预检应拒绝同一输入上实际装配会拒绝的变更；固定上游无需承诺兼容任意格式或任意版本。源码迁移采用分段等价替换，保留固定上游身份、许可证和已有优化，避免同时更换整个引擎维护方式。

## 2. 分批实施

### 本地检查点：R68-A（09-09）

实际动作桥现在要求`applied`、有原因的`noop`或`unsupported`；未知动作及必需方法异常保留错误。指针与动作不再通过通用safe_call吞掉故障，真实底栏仍执行上游enable/禁止操作策略。C++保护边界保留非字符串错误对象；独立复核发现的数值错误在pcall外转换问题已用类型判断关闭，恶意`__tostring`不执行。

- 修复后23项定向回归通过；独立8组检查通过，包含实际提取的native桥ASan/UBSan、50类映射契约、2,000次无操作、250次故障/成功交替，以及真实底栏、银行统计和编辑确认。
- 7个冻结产品/测试文件在独立复验前后完全相同。保存、加载、错误观测、saveAndExit和attach区间仍与`f95967d`逐字节相同，留给下一步顺序改造。
- 完整宿主清单从248补齐到当前发现的296项，保留原248项及冻结143项。全296项尚未运行，B/C合批冻结后执行；单项数量与场景验收分别记账。
- R68-A只确认上述宿主层级。系统键盘、完整输入玩法、ARM和真机仍待合批验收，不登记FPS收益。
- AR3隔离候选和主线程独立重跑均为19/19通过，证明有限固定源视图的完整预检/发布行为。调用者迁移、稳定构建缓存和Staff/World最终源码尚未完成，候选未合入生产代码。

原缺陷复现、第一次独立命令误选不存在测试的失败收据、修复前后的审查及所有新收据均保留，未改写历史结果。

### 本地检查点：R68-B（09-09）

保存与读取由独立Operations对象拥有本次结果，明确区分文件已提交、必要收尾是否安全、可选诊断是否失败。Platform方法成为薄入口；真实生成的SaveGame/File、原reader、发布world、菜单恢复、UI/命令行/Benchmark消费都接入同一操作边界。普通动作不会为此创建每帧诊断结果。

- 原子提交和存档格式保持；真实native writer/原reader/atomic、固定弱键GC helper、27场景及30次受控保存循环通过。必要清理失败会阻止无HID情况下的继续模拟与后续存读档；诊断故障不覆盖已提交事实或原异常。当前结果不保留旧world/错误对象强引用。
- 首次完整定向77项通过。独立复核发现正式CLI拒绝旧树过晚、repair字符串错误丢源堆栈两项，已最小修复；之后45项受影响回归及7项独立关闭测试通过。三个批次按各自源码和范围记账，数量不相加成不同测试总数。
- 最后独立冻结基线为A提交`b693677`，464文件摘要`c4cfb0566fa9b8b4e6ebcf275fb69577c82d6af6b6a08975dbc469999d2a8a5f`前后相同。文档与清单更新后再建立B提交；该开发摘要不冒充提交后的整树摘要。
- 正式CLI四个旧marker的检查已经前移到任何copy/refresh/patch之前。三种CLI模式与四种marker组合拒绝均零污染；真实旧A装配树独立验证也为零写入。完整AR3事务发布和全部调用者迁移仍未完成。
- 新增固定原始fixture两份，固定清单41→43；正式host清单296→304，旧296及冻结143完整保留。完整304套尚未运行，待C合批。嵌入与SD的Platform/Operations字节一致、实际宿主无SD加载通过。
- 本批未改变GPU、游戏速度、素材和内存预算，不登记帧率收益；完整ARM、设备运行、实际FAT/APT以及大关/长时仍待验证。原P1/P2失败及夹具错误收据保留。

### 本地检查点：R68-C（09-09）

先完整构造Operations、五个App方法及可选Benchmark，验证owner/epoch和必要接口；准备状态、激活并最后消费一次性标志后单点发布。只读peek保持标志，构造失败可重试；不可确认恢复的激活、实验资源进入或消费后发布失败锁定安全门。原方法的raw/继承身份、UI/底栏和visible精确恢复，原errorHandler参数与返回保持，观察故障不覆盖原错误。

| 真实路径 | 一起覆盖的状态与消费者 | 本批验证 |
|---|---|---|
| Platform.attach / native ensure_adapter | 五方法、Operations、App/UI/bottom身份、native epoch/active、readonly peek与最后claim、SD/内嵌加载 | 两次失败后成功、重复绑定、不同owner/epoch、真实文件与Lua/C++桥；103初次定向、30独立补充及13相邻回归 |
| Benchmark finish/cancel/tick/terminal | Activity与Stress窗口、目录/自动存档、速度、activation、媒体、Operations直接存读档与无HID模拟门、partial结果消费者 | 唯一cleanup结果；42终态/原错误/清理内重入子场景；修复后45受影响回归及18独立关闭场景 |
| 原生编译闭包 | runtime_3ds/runner完整TU、Lua真实声明与stub、frames与SHA字节转换 | 真头/stub签名和7个TU扫描；真实CMake Debug+ASan/UBSan/Werror全目标构建、native及simulator-smoke通过 |

独立复核发现的两P1已关闭：非runner finish重复restore掩盖首次必要清理失败；mark_ready引用全局早于其声明。取消路径曾因提前phase=done漏掉partial样本字段，原回归捕获后修正产品顺序；原断言和失败记录保留。所有terminal路径复用一次清理事实，必要失败同时阻止直接save/load和无HID模拟；原主错与cleanup首错分账，仅留有界字符串，实际弱引用GC证明结果未持有错误/world图。清理中的同步回调重入不会提前exit或发布。

最后冻结HEAD为B提交`dff661bfbeb41efa6d6ded99c6f3be998cec63b1`，465文件摘要`6fc866b2802f894f6a76b4d2099307e537800177957401ecde17921b0cef754e`；45项57.817秒、真实宿主构建3.668秒、两个native/smoke1.629秒。文档与清单更新后的提交另记，该开发摘要不冒充提交摘要。host清单304→310，旧304及冻结143项原样保留。各批次/子场景不能相加为独立全套数；完整310项、ARM和本批设备仍待验证。

### 影响面与验证前移

后续开改前在工单列出入口、全部实际调用者、生成/内嵌副本、状态所有者、结果消费者及完成/取消/失败/重入终态，先验证整条成功路径。首个可编译版本立即跑整目标构建和现有native/smoke，函数提取测试负责精准故障注入。独立high复核先审影响范围，再审代码；主线程在冻结前对照影响表检查遗漏。完整回归与ARM/设备合批，未变素材证据继续复用。

全套后续收口：四个旧测试文件覆盖的真实attach、Lua类型、闭文件诊断与提交边界、版本显示已同步；状态条实际R67漏改修成R68。产品的boot/状态条/log三出口一致，兼容的R63 benchmark请求协议保持。旧测试ID、关键严格断言和skip政策不变。定向冻结465文件摘要`9917d564366bb6fb050a04ea1e03e0868542f1d663436cb60120083ddc7f886f`；28项10.549秒，独立8原项与4拒绝反例分别留证，主线程全目标构建1.895秒/native及smoke1.885秒。独立首轮注入定位失败与后续单独修正分账，未重跑旧C矩阵。文档更新后的提交身份另记。

上述待验在acf1176完成：全310项309通过/1既有允许跳过，发现/选择/执行ID一致；实际735文件、305 Lua/7模块、ARM及651单函数帧检查通过。唯一设备run `20260909-202925-4bf7de94e535`正常完成，11轮窗口与2次私有存读档、原27项保护通过；故障分支仍只引用宿主证据。严格性能首尾缺口448848微秒超过既有250000微秒门，性能及完整旧档自然服务不得销项。完整记录见主计划和其R68机器证据。重构没有修改GPU算法、原速规则、媒体与存档格式，本批不登记FPS净收益。

后续顺序：独立审核AR3第一生产批的全部消费者并合入 → 同一装配路径运行已排的最小性能候选与D2首尾完整性工作 → 合批全套/ARM/同源打包/自动设备 → Staff、World最终源码及其余真实热点。每一步先满足主计划的完整链开工门；提交仍按纯重构、算法和观测分开，可分别回退。

### AR1：动作结果契约，第一批

范围：`lua/3ds/platform.lua`动作派发和`src/3ds/runtime_3ds.cpp`的Lua边界。先写下每类动作的成功、无操作、失败语义，再迁移实现。

- 成功：实际调用按约定完成，必要的重绘和状态刷新发生一次。
- 无操作：当前上下文没有合法目标、用户取消或正常禁止操作；输入批次可继续，不制造成功动作计数和fatal。
- 异常：必要接口失效、游戏方法抛错、跨语言错误；保留原异常及动作上下文，由唯一错误边界决定后续。
- 模块内部已有契约直接调用；删除吞掉错误后继续返回成功的通用safe_call。预期可选能力在绑定时明确，运行时不按一串猜测接口选择。
- 未知动作是契约错误，日志保留其身份；不宣称当前映射器已产生未知动作。

测试：真实Lua模块+真实C++调用桥；地图/菜单/建造/对话框/文本输入/mandatory pause；全部已映射动作，缺必需接口，未知动作，非字符串异常，指针down/up取消，所有输入批次在失败后的清理。正常无操作不致命、异常不报成功，错误文本和重绘次数可断言。

### AR2：保存与恢复拥有结果，单点发布App绑定，第一批

范围：从Platform提取直接的操作模块，使用现有serializer、原子提交与恢复资格；App只在一个位置绑定最终save/load/quick/error入口。

成功路径：准备操作与必要接口→执行原游戏保存→关闭并验证→安全提交→保留提交结果→独立记录/回收→返回结果。保存模块拥有提交事实，诊断只消费事实。保存前必须完成的内存准备与保存后可选观测分开。

- 构造组件和回调时不逐层改写App；完成初态同步后一次发布。失败销毁未发布状态；已发布后的detach有完整逆序清理。回滚包括errorHandler、原字段是否存在、底栏可见性、benchmark构造和native会话，不只还原四个方法。
- 保存、读取、专用R62恢复分别显式组织；保留旧档资格和独立副本，不批量解冻实体。
- writer失败时保留原主错误；诊断再次失败用有界独立状态记录。提交成功后诊断失败保持“已提交+诊断异常”，不得引导玩家重复覆盖。
- mandatory cleanup失败单列状态，并由明确策略限制后续操作；不能同时宣称整个操作完全成功。
- 暂时保持原GC时机；性能改动另一个提交测峰值与停顿，避免借重构隐式删除必要回收。

测试：真实serializer流式输出/旧reader双向相容；短写、空间不足、关闭失败、提交失败、写入后诊断失败、主错误与诊断同时失败、清理失败、一次保存/退出及两次重载。attach在每一阶段注入失败，save/load/quick/errorHandler、安装状态和原生事务均回到明确状态；两次失败后成功仍只绑定一次。SD Lua与内嵌副本实际输出一致，新增模块必须进入两条加载路径。旧17人恢复资格与行为证据保留，并对新边界做相关回归。诊断失败不递归调用同一失败出口，计时token关闭或放弃有有界路径。

AR1与AR2由一个high施工负责人依次修改Platform及C++边界，独立high复核失败语义。算法/速度、GPU图集和素材不混在该提交中。

### AR3：装配可预检、可重复、成功后发布；随后收敛最终源码

范围：`tools/integrate_corsixth.py`、integration生成函数、Staff/World等已排热点变换。

1. 预检和执行共用完整生成与最终检查。临时区域使用实际固定上游与overlay内容，dry-run不修改输入树、overlay或现用生成树。
2. 生成与检查成功才发布构建使用的结果；失败保留现用树。优先整目录生成视图和切换，编译缓存留在独立目录；若采用多文件发布，必须验证回滚与中断恢复，不能称为跨文件断电原子事务。
3. Staff先移除“旧TH3DS采样→修引用→改轮换”历史重演，维护一份最终形态。World随后迁移。业务代码放在能直接做Lua/C++语言检查的普通源文件或固定上游补丁分支；Python只保留明确装配职责。
4. 干净生产构建不承载历代现场迁移。仍需支持的旧输入列出版本白名单，在独立入口迁移；未知内容拒绝并保留原件。

实施前独立范围复核已完成，生产切换按三批放行：完整生成与全部调用者；Staff最终源码；World最终源码。第一批内部可拆小提交，调用者未全部迁移前不装机。

- 调用者覆盖bootstrap/build、runtime verifier及v2三处prepare入口、old3ds_cycle、verifier工具闭包/产品分类、CI artifact、package、test_all、共享43文件fixture及实际sound/GPU测试、本地构建和差量生成；同时搜索固定源码根和manifest读取，避免只搜索函数名。
- RNC解包需要固定原始Git源的三个文件；生成运行树与原始pin角色分开。新完整生成摘要覆盖app/world/staff/persistance及全部路径/模式/大小/SHA，并沿构建传入打包；现有overlay manifest继续保持原含义。
- fresh verifier使用自己的真实私有根运行同一生成内核，原链接/硬链接/root_id/authority拒绝规则保留。普通最终源码须进入私有快照、产品分类、工具闭包和实际发布包；无需重写已有Git bundle或release框架。
- 游戏目标clean-first已实测24.898秒，继续保留并复用工具链、依赖、媒体及构建目录。仅需明确构建owner、当前完成视图和成功收据；复杂mtime与失败尝试增量状态机暂缓。
- Staff同时收走spatial/litter与staff_hotspots的写入，保留独立reception绑定；World同时收走world_profile与entity_index的World写入，保留索引模块和原采样/RNG/保存语义。逐文件完整生成等价与真实行为门分别覆盖。

测试：固定上游正常生成、重复生成字节一致、预检/执行接受或拒绝一致；合法空白造成锚点失配时两者都拒绝；每个变换故障零污染；目标存在/不存在、只读源、部分旧树、磁盘/发布失败、链接与路径边界。迁移前后生成产品源码逐文件差分，严格Lua实际执行、ARM链接和嵌入身份检查。删除旧字符串断言前，必须有对应真实行为测试。

该工作与AR1/AR2可独立研究；集成按顺序，避免装配器与Platform同时变化而失去参考。纯装配等价变化不单独要求一轮人工真机；纳入下一次候选自动回归。

09-09私有交付状态：`AR3-production.patch`为95921字节，SHA`55a7d4ff55c995b3065ff1ed7bf07ea43c1daec240281255b3d48cb5a7bcc8ef`，基线acf1176。施工报告为735产品文件字节/权限等价，305 Lua/7模块、受影响宿主组和native通过；原310及143保留，新增10个生成契约ID。主线程已核对补丁哈希并阅读消费者表，独立代码审查、正式ARM/full320/打包和三份本地调用脚本启用仍待完成，不合并各测试组为独立总数。真实RNC精灵的native解码成功与压缩SOUND全包未覆盖分别记账；Linux CI及新authority未执行。主线和共享现场尚未切换，Staff/World尚未迁移。

09-10补充：AR3上述私有交付段保留历史状态。v3生产取消域独立通过，v4仅修两个根取消测试的退出清理；三个信号上层中断后均无域外活进程。主线程核对29条合入路径与v4冻结清单一致，仓库与本地三份调用者已对齐。完整清单经真实发现合入D2后为327，AR3原325摘要失配在此阶段修正。最终候选前的清单加载、真实生成及整目标检查先完成；提交数量不会增加完整回归次数。Staff/World最终源码、AR4–AR6仍待后续逐项推进。

### AR4：按状态和存活期提取Runtime/Platform

保留现有GameView、LifecycleController、GPU renderer及保存组件。会话入口只组织初始化、连接和停止；输入拥有输入队列/取消/坐标，呈现拥有画布/视口/资源栅栏，保存拥有文件操作结果，诊断拥有记录与样本。

一次提取一个所有权边界；调用方使用直接有限接口。不引入容器、事件总线或插件注册中心，不把所有状态搬进一个共享context大对象。Old 3DS大对象继续静态/稳定存储，禁止把原静态大块搬上主线程栈。

验收：初始化/失败/重复初始化/停止顺序、HOME/合盖、输入捕获取消、GPU命令在用/释放、保存时暂停、内存错误、堆与ARM栈上界；CPU计数边界及有序渲染像素相符。每次提取独立中文Git，可单独回退。

### AR5：清理无消费者的调度和小契约

FrameScheduler仅保留旧Panel所用刷新门控，普通Game路径不推进该旧门。SimulationClock和PresentationClock继续保持原职责及速度语义。只初始化的save_installed/load_installed已在C提交5300c79移除，本次acf全套及设备正常链已覆盖，直接销项；其它旧门和已忽略的内部光标参数仍待收敛，更新所有真实调用者及嵌入副本。

测试覆盖首次刷新、重绘请求、脏状态、按住笔、暂停恢复、时间倒退与大跨度、Game模式无旧门副作用；全消费者搜索和ARM链接确保没有遗漏。现有测试保护的是行为，旧结构断言随契约收敛。

### AR6：实验资源与开发编排隔离

正式目标只装配玩家loose路线需要的组件；实验RuntimeSession仍有独立目标/测试。先列消费者与构建闭包，再移动文件和链接范围。开发runner与版本专用恢复和玩家入口边界明确；可复用的保存保护和必要恢复继续在正式代码。

验收：正式/实验分别构建；正式loose冷启动、保存和生命周期不依赖实验挂载；实验原红/绿用例保持；未使用分支的删除有消费者和符号证据。保持toolchain/素材缓存复用，不重建验证框架。

## 3. 测试和性能工作如何穿插

| 层级 | 必须拿到的证据 | 复用边界 |
|---|---|---|
| 单提交 | 成功路径和错误契约对照、源/嵌入一致、受影响真实Lua/C++测试 | 不以文本匹配或mock单独替代真实边界 |
| 合批 | 严格Lua装配、ASan/UBSan相关测试、ARM编译/链接/单帧栈、输入/保存/绘制回归；跨语言/保存结构合批结束跑一次完整现有宿主套件 | 纯行为保持重构不修改RNG/日期/保存图；性能算法提交分开；全套不随每次文件移动重复 |
| 自动设备 | 健康医院正常模拟、中文与媒体常态、窗口/输入、私有保存重载、原27项保护和launcher回返 | 无变更的媒体资源矩阵复用；不为每个文件拆分单独上机 |
| 交付 | 忙医院/大关、换关、正常及最高原版速度、HOME/合盖、长时、实际听感/触控 | 软件/宿主通过不替代物理验收；将必要人工操作合并 |

重构收益登记为状态归属和路径收敛、正确性、可测试性；帧率收益另列实测。R67后仍保留B2员工、B3温度、A3/C2绘制提交、E2/E3保存与冷窗口/启动、D2/D3调度同步全部既定工作。先完成高风险契约，再推进最贵入口；互不重叠的离线探索可并行，跨边界改动顺序集成。30FPS参考不作为必达条件。

目前R67已证明本轮健康路径，约15.297FPS，Normal实际World45.840Hz低于名义55.556Hz；未证明硬件上限。优化的实际任务继续是减少完成正常游戏所需的工作和开销、降低长停顿、保障存档及容量。收益不足的试验保留测量和回滚，避免以完成清单为由引入多余框架。
