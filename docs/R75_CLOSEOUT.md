# R75 集中收尾结果

2026-09-11。本轮代码施工和自动装机完成，主运行流程完整结束；完整历史容量协议仍为 **NOT_PROVEN**。当前进入实际游玩观察，不追加理论优化、双屏改造或人数凑整测试。

## 1. 固定身份

- 实际装机源码：`bbb6fe5fccb004c41b8dfdff67fd7ed5656f9ae2`，tree `a52f9152c4d0660974089aa5c0407a42b7976b07`。
- 3DSX：2,774,600 B，SHA-256 `38d58b800378e5f3595b28c4c78c00ace00740ac155dce1898d3213e29835ff9`。最后一批只改外置Lua及身份文件，复用相同程序。
- 最后差量3文件、151,909 B，部署及读回163.890秒；全部45份当前玩家存档/配置前后哈希一致，发布文件返回后亦一致。
- 真机run：`20260911-033949-f36ca58ce329`。manifest SHA `d7b2a02332290484cc49be0c8d9fbfc614098964e6af9f5c8a639a254959b3fb`；result SHA `4d3c91f373599a651e23440c763a383336cea043d1cf4ba9c5098837fbe47af1`；返回boot `e7a83cb7508b87eaaf32b7a5d15f9676`。
- 玩家输入副本SHA `d459ae706daa418393b22e56c9e253142ebf5d0383df3bcf5d837172c1f4fe00`。测试产生的全部保存位于runner私有目录，未覆盖玩家原件。

## 2. 本轮已实测的成功路径

以下为有绑定的设备原始阶段事实；完整协议状态仍见下一节，不能将阶段事实写成全部门槛通过。

| 范围 | 本轮结果 |
|---|---|
| 启动、退出、runner回返 | 设备终态PASS / COMPLETE，新的launcher回执完整，游戏错误0 |
| 繁忙医院 | 41员工、14房间；保存前60.002秒，保存后60.340秒。两段41人均持续更新、身份一致，无员工更新失败；40人有动作覆盖，不推导所有岗位都完成服务 |
| 真实保存窗口 | `UISaveGame.confirmName/trySave/doSave`提交、重载指纹一致，busy阶段原始PASS |
| 第12关 | full关载入并运行60.384秒、785帧、2353次World更新，私有保存/重载指纹一致。该关为初始空医院，不冒充末关繁忙经营或自然晋级 |
| 返回原医院 | 返回同一example.level，17员工、9患者、14房间、起始日期一致，再运行5.013秒、226次World更新；最终私有completed.sav为415,059 B，保存完成后正常退出 |
| 保存 | 19个save-begin/complete完整配对，范围2.501–3.334秒；最后一次2.654秒。ABBA外层完整操作的时间另列下文 |
| 错误与观察 | FATAL/空队列/医生交接异常无新增记录；log_failed=0、log_truncated=0，Lua分配失败0。这不等于穷尽全部游戏行为 |

第六轮卡在末关返回前的测试许可撤销时点，本轮修订已经在设备跨过。真实UI构造、原生保存/恢复、afterLoad、C:tick/C:load及成功/拒绝/失败清理的组合回归一并保留。

## 3. 性能收益及资源边界

同一暂停状态的16/64/64/16KiB四份保存完全同字节：均415,906 B，SHA `19fb5841da77d0aabe9c7390fef2058c35ccff21ac9701c0d01c8f3d9405b422`，第四份已真实重载。

- 16KiB外层操作3411/3074ms，均值3242.5ms；64KiB为2699/2637ms，均值2668ms。本批平均减少约17.7%，与之前多批约15%–17%的方向一致，保留64KiB默认。额外48KiB仅为临时普通堆，不占线性内存。
- 不能宣称所有写入延迟降低：本批最大单次fwrite为16KiB 58.394/61.665ms、64KiB 68.866/69.327ms。保留默认的依据为整体保存时间；旧十秒级长尾未复现，继续列已知风险。
- 繁忙两段约9.816/9.745FPS，World约29.399/29.251次每秒；Normal仍追不上名义时钟。此次集中收尾没有证明持续FPS净提升，30FPS不作硬门槛。
- 普通堆最低估算16,494,656 B（约15.73MiB），终态25,475,368 B；线性余量2,199,552 B（约2.10MiB）稳定。Lua峰值14,576,947 B，分配失败0。
- Lua旧对象完全释放仍NOT_PROVEN，原生GPU完整释放及最大连续可分配块也未证明。单次低水稳定不能替代长期泄漏验收。

## 4. 读回工具修订及未覆盖项

首次读回将“返回场景变化”判为FAIL。固定上游Map:load的自定义文件分支保留difficulty=nil，Lua摘要因此省略该键；本轮前后都为同一example.level、相同14房间，两个difficulty均缺省。

只修电脑侧比较器：绑定的`.level`文件场景允许难度成对缺省；原版编号关卡仍须有明确难度。前后缺省不对称、关卡/房间改变、难度改变及必要字段缺失继续失败。11项读回测试（含10种新场景变体）通过0.471秒；旧FAIL原件保留，以相同不可变run重新分析。工具及文档后续提交不改变装机代码，不重编、不重上传、不重跑设备。

修订后完整协议为NOT_PROVEN、errors为空，唯一缺失组为历史双接待员第二服务窗口。旧连续性档只有一台接待台，source20没有岗位，且自然运行发生两名员工离开；第一窗口17人都有更新，旧17人身份连续性无法保持。该历史门保留未通过，不制造岗位服务、不改变工资或存档。

其余边界明确保留：

1. 旧三次医生警告是否全部属于已修的不可达交接路径未直接证明。相关真实动作/房间受控回归通过，本轮无新警告。
2. HOME、系统键盘取消、新中文/姓名排版仍需实物观察。旧R74的普通键盘确认和基本合盖恢复证据复用，不要求重复。
3. 两小时运行、长期重复换关释放、末关繁忙经营及自然晋级没有本候选完整证据。
4. 正式Fresh Chain最终封版作业未执行；不能标为正式封版PASS。

## 5. 代码与验证收口

- 已集成：房间员工接管/不可达指派修订，勤杂工等待队列幂等性；原子保存及失败保护、64KiB缓冲；音乐结束事件代次与正常换曲；中文缺键/提示/姓名显示和字体覆盖；加载三行中文、启动光盘双屏呈现、暂停提示及系统键盘入口。具体实现和边界沿用R74/R75工单，不伪造新的视觉验收。
- 最后产品改动的28项相关回归PASS；生成1.968秒、ARM整目标11.994秒、宿主原生目标2.464秒、装配初始化验证0.411秒，源及7项产物绑定保留。
- 装机提交[GitHub CI 34521276407](https://github.com/zzliu-coder/corsixh3ds/actions/runs/34521276407)十项实际执行作业全部成功，Fresh Chain封版项跳过。随后只读工具的11项本机回归独立记录，不继承该CI成绩。
- 当前停止重复容量跑批。保留本轮正常运行、保存和原件保护结果；后续由实际游玩中明确复现的错误或明显卡顿触发有界研究。实物项目可在下一次正常玩时顺手确认，不再安排41→43人的重复压测。

本机证据根：`work/iterations/20260910-r75-handoff-io/`；装机`package-10/deploy-10`，运行`device-07`，旧读回`analysis-07`、修订读回`analysis-08`，产品回归`local-12/build-10`，工具回归`local-13`，CI原始收据`ci-bbb6fe5`。原始存档和完整私有日志留在本机，仓库仅提交实现、回归与脱敏总结。
