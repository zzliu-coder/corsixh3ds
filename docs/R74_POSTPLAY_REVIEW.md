# R74 新实玩、独立复审与剩余收口

更新：2026-09-10。本文接续 71d37b4 的部署与实玩，当前排序以此页和总计划页首为准。旧自动测试的停止、R73 保存错误、R74 新手动会话分别保留，不能互相替代。

## 1. 新证据与明确边界

本次从 192.168.8.238:5000 只读取得 89 文件、24,723,765 B，耗时 155.342 秒，未投递新 run、未写设备。新手动 boot.log 为 625,796 B，SHA-256 `5720b6f844b7631359b6dec1995485d227f1cad78cfb48c059ebb8d02d3f803c`。R74 程序与已部署 71d37b4 制品一致：2,745,996 B，SHA-256 `203886ca35ffee6d50fa3929944b0ec79824195c6d3e0bb3cfc7347c8a2793bf`。原始日志及用户存档只保存在本地证据目录。

| 检查 | 新手动会话结果 | 尚不能推出的结论 |
|---|---|---|
| 游戏运行 | 17→41 名员工、8–15 名病人、14 个房间；10,142 次模拟派发全部完成，计数失败 0；无 FATAL，正常 shutdown | 不替代逐员工服务验收；员工规模增加，患者负载仍中等 |
| 保存 | 9 次完成，包括键盘输入后的 00004、00009、覆盖、自动保存、保存退出；原子提交均 ok=1 | 当前最新文件尚未完整游戏重载；写入成功与重载成功分账 |
| 键盘 | 两次 begin/end，对应返回 button=2/result=2，随后以新名成功保存 | 本次未记录取消、非法输入、HOME 物理按键；这些主机负例和真机成功路径分开 |
| 合盖 | flags=0x0c 对应 Sleep/Wake；其后模拟、输入与保存继续；另两组0x03与系统键盘相邻 | 不把键盘 Suspend/Restore 当成用户按 HOME；声音和 LCD 恢复观感仍需用户反馈 |
| 内存 | 普通堆最低采样估算 18,333,072 B（17.48 MiB）；线性常态 2,199,552 B（2.10 MiB），未见分配失败 | 总余量不保证最大连续分配，也不证明大关或长时无泄漏 |
| 性能 | 混合最高速度、招聘、睡眠、菜单和保存；保持原五档速度，中文/中文语音/音乐开启 | 不能把此轮混合窗口均值当 Normal 基准或净优化率；硬件 CPU/GPU 利用率仍未知 |

当前 `00009.sav` 为 410,191 B、SHA `b5ac0e84573a61e5869f06b441c8f0cff474e2cb99894bf059dc1aeea06c6388`，非执行图解析完整消耗字节，包含合法急诊 UIWatch 与正确 owner/登记关系。`save-and-exit.sav` 为 415,042 B、SHA `d459ae706daa418393b22e56c9e253142ebf5d0383df3bcf5d837172c1f4fe00`。新存档已本地保全，不能用前轮 SHA 覆盖恢复。

## 2. 警告要按来源与个体分开

新 gamelog 文件内有 5 次空行动队列警告和 1 次重复注销员工回调。对照起始 Slot2 存档内的 world.game_log，后两类中的病人一次、护士一次和重复注销已由旧档带入；本次新增是同一医生的 3 次 call_checkpoint 空队列恢复。医生三次坐标不同，说明其间有移动；这仍不等于所有医疗服务正确。

扩大审查仅覆盖行动队列、timer、呼叫交接及存档内日志来源。禁止因为全局 tick 完成就忽略个体问题，也禁止把启动时输出的旧存档日志全部记为新故障。

另找到独立、可复现的勤杂工问题：当前永久 MeanderAction 执行时，无任务路径调用 doMeandering；原 isMeandering 仅识别 `[idle/walk, meander]`，漏掉当前 `[meander, ...]`，反复追加等待动作。新旧两份保存中同一 name_seed 的队列可从 136 增至178、24增至55，支持持续积累。固定上游同样存在该逻辑。施工限定为当前无限闲逛的幂等排队，保留任务搜索/RNG频率、有限动作、房间退出与派工；不清空旧档动作，不全局放宽 isMeandering。

## 3. 已知自动验收停止根因

自动 run `20260910-201202-a288d6f71d9b` 仍为 NOT_PROVEN/NEEDS_INPUT，停在 continuity.sav 载入后的窗口检查。普通手动游玩没有触发该私有验收检查。

持久化后 Watch/Annual 实例元表可能重建，方法类表通过 permanent 保持身份。旧检查比较实例元表地址，拒绝合法窗口。修订比较直接 `__index` 的精确类身份，继续检查 owner、modal登记、原方法/按钮回调及一次性结算；不采用可接纳子类的宽泛判断。Button 元表已由真实 Window permanent 登记，测试保留这一差别。未知窗口仍停止，并最多输出一条有界原因摘要，保留 `TH3DS_NEEDS_INPUT` 终态协议。

新增真实 strict/class、原生 dump/load、完整永久对象登记、真实 UI afterLoad/Watch/Annual/Button 正反例。主机图形、声音、World/医院服务仍是明确替身；完整医院连续重载和后续容量必须真机验证。

## 4. 外部复审取舍

输入：《Corsixh3ds_R74_复审报告_71d37b4.md》。已核对本地固定源码及保留 CI 证据。

- R73 默认目录、嵌套构建器和短写 errno 合同修复可保留；真实保存窗口未声明 IS_3DS 故障在新手动保存路径未复现。
- 接受 R74-A01：varint 测试重载 new 却缺 sized delete。补 scalar/array 两入口；测试显式启用 sized deallocation，保留 ASan/UBSan、字节等价及原7047→0分配对照。其他两份重载分配器探针已有成对释放入口。
- 接受 R74-A02：CI 构建 manifest 列出 runtime-stack-proof，但上传路径未包含它。补上传名单与现有工作流测试；实际新 CI 下载包完整性留给下一 CI 验证，不伪造远端 PASS。
- 外审没有覆盖最新手动日志，也没有证明完整游戏无故障。其“未发现新P1”仅保留为所审范围结论。
- CI 与本地设备二进制不同：CI 2,802,520 B，设备2,745,996 B。同提交不等于同制品。

## 5. 中文与姓名收尾

截图英文来自 `adviser.warnings.cannot_afford_machine`，含义是更换机器所需资金。中文表缺少对应新增项，语言继承回退到英文。正式中文补充层覆盖同类菜单、提示、字幕；中英切换及原占位符保持。扫描现代英文新增表、语言加载合并与3DS普通操作提示，原始 LANG.DAT 的全部语义校对单列，异常堆栈、调试协议和用户自定义存档名保持原文。

姓名旧批仅解释上游缩写规则，未改显示。StaffProfile只存 initial 与组合姓氏，没有可恢复的完整首名。本批中文且CJK initial时显示既有完整姓氏，英文/Latin initial维持原样；不改角色字段、随机数或存档身份。列表和详情使用同一显示入口。

本次设备配置实际为 `language = [[chinese (simplified)]]`。原存档槽及本轮早期草稿仅比较 `Chinese (simplified)`，会漏掉实际菜单保存的小写配置。现由 `3ds.media.isChinese` 统一大小写和正式别名判断，姓名、存档槽、语音菜单和native提示共用；不回写用户配置。测试使用这份真实小写拼写及中英切换，保留原错判负例。

补充313个现代英文语言表中的中文键，逐项核对键及占位符。19条普通native HUD提示用显式ID和固定mask，新增14,225B只读alpha；复用CPU/GPU提示条，无新GPU纹理或运行时字库。小型media模块的内嵌preload一并闭合，无SD、无lfs情况下可加载平台并同步中文状态。系统键盘applet内部Cancel/OK及字符规则提示仍沿系统字体的英文显示，尚未取得该系统中文字形覆盖证明；未知错误、日志和benchmark协议保持原文。

字库继续使用同一 HarmonyOS Sans SC Medium，仅增加所需字符。主机验证原字形轮廓及advance不变；新子集358,784 B，旧350,012 B，新增41个码点（最终合批再核覆盖）。音源、采样率、播放策略不变，无需重转或重跑旧语言/音乐性能矩阵。

## 6. 保存长停顿仍开放

本次9次保存耗时2.749–12.426秒。最长一次 dump 11.359秒，其中 fwrite调用累计10.213秒、最长单次809ms；dump内剩余约1.146秒，close4ms、原子提交158ms。另一保存退出9.603秒，其中写入7.390秒。相近约400KiB的保存也有约2.8秒成功，不能按文件大小解释全部波动。

这些是stdio调用墙钟时间，尚不能区分SD固件、文件系统、小块拆分及其它等待。下一研究只比较1–2个有界写出方案，固定私有存档/同一入口，保留临时文件、短写/flush/close失败、备份及原子提交。不关闭自动保存、不异步读取正在变化的游戏对象、不靠减日志隐藏错误。

## 7. 施工与验收顺序

| 顺序 | 工单 | 销项标准 |
|---|---|---|
| 1 | R74-05A/B 窗口身份与真实持久化验证 | 正反主机链通过；新私有run跨过continuity加载并继续容量 |
| 1 | R74-A01/A02 外审两项 | 分配探针保持字节契约；构建上传清单完整，新CI结果单列 |
| 1 | R74-08 勤杂工队列 | 真实无限闲逛循环不再累积，任务/RNG/有限动作不变；旧队列不破坏；繁忙存档私有复验 |
| 2 | R74-09 中文与姓名 | 缺键/占位符/语言切换/原姓名字段/字体覆盖主机通过；高频界面与正常native提示待合批观察 |
| 3 | R74-02 保存IO | 固定私档受控比较最长停顿、总体耗时和内存；收益与失败保护同时通过才保留 |
| 4 | R74-05C 容量/服务/换关 | 41人旧档正常服务、原保存UI、末关加载及返回、自然接待；无法到达的阶段保持 NOT_PROVEN |
| 5 | R74-06/07 最终收口 | 中文Git、一次最终组合编译、差量部署并核身份；仅补未覆盖的物理项目，不重做已完成的普通键盘确认和合盖基本路径 |

本页是滚动清单，任何“主机PASS”都不自动升级为“真机PASS”。新施工后的设备版本、部署、回读和新CI尚待登记。

## 8. 本轮组合验证与交付边界

冻结合并源码后的相关边界批70项全部PASS，44.281秒，测试前后所有已登记源文件哈希一致。覆盖原生持久化窗口身份、行动队列、汉化姓名、真实键盘/保存入口、平台无SD回退、呈现、输入、保存内存及构建脚本。另7项媒体入口/FreeType/SDL_mixer测试全部PASS，10.582秒；没有重转语音音乐，也没有重跑旧真机媒体性能矩阵。

完整宿主清单由376增至385项，保留冻结基线与skip政策；实际发现的385个ID和清单逐项相同，排序哈希为 `0565f4ef792d53255d0286609af32f0fbd00717a5a1958e4ad9310f0b55431da`。本轮执行上述相关批，未宣称新385全套通过；71d的旧全套成绩不升级为新候选成绩。完整生成、ARM链接、最终栈/堆证明及设备步骤继续分账登记。

本地证据根：`work/iterations/20260910-r74-capacity-finish-audit/`，子目录 `manual-readback-02`、`newqueue-audit`、`postplay-font-01`、`postplay-local-01`。用户新存档与完整日志保留在本机，Git只包含实现、测试及脱敏总结。
