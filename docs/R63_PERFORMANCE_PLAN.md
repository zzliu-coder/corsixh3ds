# R63 起：状态正确性与五框架滚动施工表

更新：2026-09-09。执行顺序以本表为准；单项状态必须由产物更新。目标为中文、所选语音和音乐常态开启，在规则正确的医院达到有效 30 FPS+，同时缩短弹窗、存读档停顿。目标尚未达到。

## 当前执行面板：R66自动读回完成，性能仍未达标（09-09）

装机产品 `e6a6c85b0e9a1eec7bd81f652e00bce12e91e6c3`，程序SHA `ad12eec3a4951b95169126e5bfe0625a2b5114a833aeef1aa7d2869d21155978`。61项产品相关测试、304份实际Lua装配、ARM及646个移植符号单帧栈检查通过；8文件3,142,118字节差量安装、唯一run `20260909-144838-91c4b8083722` 和新launcher回返通过。原25份存档及配置/热键共27项运行前后fresh SHA一致。仅本地中文Git，未推送GitHub。[R66机器证据](evidence/r66-runner-20260909.json)。

| 项目 | 本轮事实与边界 |
|---|---|
| 有效画面与模拟 | 60.060241秒，762帧、2286 World、762游戏小时、25033实体更新、errors=0；**12.687 FPS，30FPS目标FAIL**。P95直方图上界262.143ms、最大730.211ms。相比R65的15.595FPS更低，净收益未证明，不能收口成性能改善 |
| 同窗时钟 | 38.062次完成World/秒，Normal名义55.556；丢弃18.895731秒欠账，首尾各90ms，0失败、0rebase、755次预算退出。实际时间推进低于名义速率；继续减少成本，速度规则保持 |
| 旧档连续性 | 原17名员工在保存前25秒窗各成功更新332次，保存重载后25秒窗各381次；16名有动作/路径/位置推进，15名非接待员工均有timer与动作证据。在岗接待台更新正常，但2名接待员都无自然服务，完整接待与整院未来行为NOT_PROVEN。资格、立即往返及观察后往返PASS |
| 健康压力和保存 | 210.652秒、11轮窗口、2次私有保存重载PASS。10条closed-writer记录为361747–389259字节、23–24次flush、1.951–2.649秒；writer记录不代表外层atomic commit。原档保护和实际重载另有证据 |
| GPU提交 | 同窗542613个pieces中233701次驻留页切换（43.07%），268次上传、2次eviction checkpoint。选中样本的C2D均值：不切页4.26µs，切页20.90µs。采样计时不乘64外推全窗；这是CPU提交，硬件GPU利用率未知 |
| 地板与内存观测 | 地板第一遍8.259秒，约10.84ms/帧，含相邻阴影及遍历，是整段可省成本的上界。内存观测2035次/3.152秒；sound_read1762次，约87%，含2/4字节读前读后强制扫描 |

R66温度累计5.000秒比R65少，实际温度更新也从936次降到762次；逐次约6562µs对6501µs，**没有真机净收益证据**。当前保持等价算术，后续需固定工作量对照。最低普通堆估算余量20,917,000字节（约19.95MiB），不证明同等大小连续块可分配。

离线新读回器9项正反例通过，原R65基础分析逐字段保持。真实native Lua日志带`lua-diagnostic:`前缀，首次离线扩展漏识别；原始日志和首版派生结果保留，修正仅识别已知传输前缀后生成`run-analysis-r66-v2.json`。结果未改变设备结论：17人真实更新已证明，接待服务仍未覆盖。缺行、半窗、计数冲突不放行。

### R67及下一阶段排序（原ID继续销项）

| 优先 / ID | 下一项最小施工 | 放行标准 |
|---|---|---|
| 1 / D1、C3 | 仅对成功小块`slice_read`的before/after观测复用250ms节流；实际解码准入继续fresh | 4字节/EOF/截断/失败/256KiB阈值/unknown请求及堆骤降后准入的真实入口回归；读取内容和PCM不变，失败与事务边界不丢 |
| 2 / A3、C2 | 现有三页atlas内试验地板软优先：优先共页，其他绘制仍可借用全部容量，不移动已命中图块，不重排draw | 新增纹理0字节、固定状态有界；floor内部切页计数、generation与在用页回收、溢出、阴影/墙/人物及双屏顺序等价。冷启动同条件观察切页/上传/驱逐，负收益不默认投产 |
| 3 / D2、测试可比性 | 健康医院性能路径与旧档恢复覆盖分账；补固定模拟工作量的初始校准并记录计数/相机/日期，保留真实时间吞吐窗 | 保存内既有RNG恢复语义不变，不改Normal、Max等速度；固定30秒预热会到达不同游戏日期，不能据单轮小差异推算净收益；观测开销与产品收益分开 |
| 4 / S2、S3 | 已证实17人持续更新与往返的原件留存；2名接待员自然服务、更多后续实体和业务继续列未覆盖 | 用后续自然排队场景补证，不凭站立不动判FAIL、不代答玩家选择、不为同一25秒等待反复重跑。原存档始终保留 |
| 5 / B、D、E后续 | CPU实体/温度逐真实热点、呈现与模拟调度、弹窗/保存/启动停顿；按新增同窗证据选择入口 | 30FPS与名义模拟速度同时达标；有序渲染、中文/语音/音乐、存档保护不退步，再做忙医院/大关/换关、HOME/合盖和长时交付 |

R67先完成相关电脑反例和实际集成，批次中文Git后再自动差量与同条件读回。媒体未改部分复用已得证据；资源和工具链不重复下载。本轮不实施大块地板纹理缓存、通用渲染重写或自定义速度，后续沿五框架滚动推进。

## 历史执行面板：R66合批施工时点（09-09）

本批从R65产品及55148c0文档里程碑继续；3个GPT-6 Astra medium子流程施工，主线程接通真实World/平台错误边界与同窗统计。常态中文、中文语音和音乐单profile保留，未变媒体矩阵复用，不重新下载工具链，不推送GitHub。

| ID | 本批实现 | 当前验证与继续条件 |
|---|---|---|
| S2/S3 | 精确R62副本资格→暂停私有往返→25秒Normal自然观察→暂停保存重载→第二个25秒观察→健康性能基线；逐原员工独立计数 | 宿主真实Entity/接待服务、17人逐个推进、原生两reader/流式writer/对象图隔离通过；真实17人持续工作待设备。合法等待或自然无服务为NOT_PROVEN，强制选择窗口停为NEEDS_INPUT |
| D1/D2 | 模拟clock与呈现/CPU使用同一微秒边界；输出实际steps、completed、failed、debt前后、drop、rebase及预算退出差量 | 500ms日志、错误/取消/时钟倒退、计数重置和GPU开/关桥接验证；输出在窗口关闭后。补账规则与速度档位不变 |
| D3/A3 | GPU固定≤512B采样，每64次按帧轮换，分bridge/几何/atlas队列/C2D提交；地板第一遍粗计时 | 宿主实际GPU有序像素trace开关一致，裁剪/透明/翻转/队列/图集回归通过；实际采样ticks不乘64假装全窗总量，观测自身开销和热点待读回 |
| B3 | 仅医院16位自然冷却替换为精确整数式；完整扫描、双槽和失效规则保留 | 全65536值与真实地图回归通过；不启用收益不确定的活动块/条件发布，真机净收益待验 |
| E2/C3 | 16KiB有界writer保留，成功关闭后通过既有diagnostic_line留下bytes/flush计数 | 失败/重试/旧reader/原子提交回归通过；行内close_ok=1、commit_included=0，最终提交仍看原提交事件 |

合批首轮61项执行中60项通过；旧基准夹具仍预期恢复往返后立即进入健康基线，需更新为先进入自然观察，保留损坏往返的拒绝断言。更新后重跑整组，随后实际装配/幂等、ARM、中文Git、差量及一个新run。这个记录是施工状态，不能作为装机或性能通过证据。

下一排序由R66同窗真机数据决定：CPU提交与地板占比决定A3保序命令/几何复用；World/Entity/温度决定B2/B3逐热入口；C缓存按实际驱逐与重复上传；D调度同时检验原速与长帧；E保存/启动仍按峰值和停顿下降验证。任何新机制必须先有收益依据和失效契约。原版FPS参照见下节，30FPS+仍未达到。

## 上一批执行面板：R65自动验收结果（09-09）

装机产品 `610cfe9f568d5b1eaf9f162c76dbe59c46bc279c`：有界FILE保存、严格接待员恢复资格、低频普通内存观测。55项/19模块相关回归、实际303份Lua装配、ARM构建通过；6文件/2,973,295字节增量安装通过，资源和工具链复用。唯一run `20260909-134826-bc791b0f8be5` 已自动运行和回返，errors=0；原25份存档及config/hotkeys共27项fresh SHA完全不变。[本轮机器摘要](evidence/r65-runner-20260909.json)。当前只做本地中文Git，不推送GitHub。

有效60.021092秒：936帧、2808World、936游戏小时、30383实体更新，**15.595FPS**。P95直方图上界131.071ms，最大674.638ms。R64为15.410FPS；两轮预热后工作量未完全锁同，净收益精确A/B仍NOT_PROVEN。普通内存观测在本窗为775次/1.156秒，R64为1445次/2.285秒，观测负担下降，整体流畅目标仍未达到。

旧档17名员工通过完整资格，并完成恢复副本的立即保存重载；**恢复后持续工作仍NOT_PROVEN**。本轮恢复往返后直接加载健康基准，未观察旧世界未来行为。健康压力208.835秒、11轮窗口、2次私有保存重载通过。7个原生保存观测边界约2.52–2.98秒（含提交），writer段约1.55–1.57秒；设备缺少Lua `save-stream`计数行，实际bytes/flush_count不据文件大小倒推。本轮普通堆最低估算余量22,498,616字节，连续大块余量未证明。

按用户要求，Luna max完成[原版FPS与目标校准](FPS_TARGET_REFERENCE.md)。原版1997年DOS/Windows的固定FPS未取得可靠数值；固定CorsixTH Normal名义实体/动画更新约18.52次/秒，基础World为55.56次/秒。**30FPS+目标保留，同时验收原速模拟和长帧。** 不改上游速度规则，不用重复旧画布或停止模拟制造高FPS。

### 下一批施工顺序（沿用原ID）

| 顺序 / ID | 最小真实入口与工作 | 销项条件 |
|---|---|---|
| 1 / S2、S3 | 将旧档恢复→暂停保存重载→恢复正常运行分成阶段；对原恢复对象记录成功更新与行动/计时推进，接待台记录实际服务 | 副本专用、原27项不变；合法等待单列，自然未发生服务标未覆盖；错误/需玩家决策时停；监控不进入保存对象图 |
| 2 / D1、D2、D3 | 现有steps/completed/failed/debt/dropped/rebase在同一样本边界快照；用≤512B有界采样拆分GPU bridge、几何、atlas/队列及C2D提交，标地板第一遍 | 不重复设计日志框架；无逐draw IO/分配，采样开销可测，呈现序列/像素不变；同窗成本决定下一项渲染优化 |
| 3 / B3 | 医院自然冷却的16位精确整数式，保留完整温度扫描、双槽和失效契约 | 已有研究65536值穷举/双槽ASAN-UBSAN通过；正式生产内联ARM与真实温度净收益另验，不能将局部7→5指令宣称整体提速29% |
| 4 / E2、C3 | 保留成功的16KiB输出缓冲，补入已有可收集诊断出口；关闭/提交与计数边界明确 | 正常保存、失败重试、旧reader、原档保护不退步；输出缓冲上界与整个对象图峰值分账，不为缺一行日志单独上机 |
| 5 / 合批读回 | medium子流程施工/测试/读回，主线程分析和中文Git；相关回归、增量ARM、一次差量候选跑上述新覆盖项 | 复用未改媒体矩阵；相同输入/速度/镜头/样本窗口，结果分层。下一优化由热路径实测排序 |

温度活动块和条件发布均保留离线原型，暂不默认投产：活动块在稳定混合地图宿主快17.6%，冷启动加气温变化慢1.9%、全动态慢4.3%；收益依赖真实活动分布。稳定地板纹理缓存约需新增2MiB，也先看实际第一遍绘制占比。现有O2、C2D零旋转快路、全sprite快路、图集和翻转/文字缓存已存在，不重复记为新增优化。

后续仍有A/B绘制与模拟热点、C窗口/资源工作集、D输入/同步、E启动和长停顿，以及忙医院/大关/换关、保存连续性、HOME/合盖、完整长时和最终交付。R65只关闭本轮实际覆盖项，整个游戏目标未完成。

## 历史执行面板：R64 真机读回，R65施工时点（09-09）

R64 产品 `4ca9f462ede2e17770e373aa08c9982f3f736dd6` 已安装并完成一轮自动测试。有效60.025475秒样本为 **15.410 FPS**：925帧、2775次World、925个游戏小时、29858次实体更新，errors=0。P95帧间隔直方图上界262.143ms、最大651.478ms。11轮窗口及2次健康副本保存重载、自动返回runner通过；完整22分钟、大关、旧档恢复未来行为和30FPS仍未销项。[脱敏机器摘要](evidence/r64-runner-20260909.json)。

本轮普通堆最低估算余量约21.34MiB。原25份存档与config/hotkeys共27项fresh SHA不变。首次差量末段超时后按已验证新旧文件续完切换，失败原件保留；下一批以 `deployment-recovery-journal.json` 为安装基线。未修改媒体的矩阵复用，当前仅本地中文Git，不推送GitHub。

离线解析v2按实际原生调用顺序排除起点前247µs诊断输出，结束边界仍精确、覆盖门槛仍250ms。18项回归通过，旧R63两份分析完全不变；原解析失败保留。此改正没有另跑真机。R64与R63的预热后工作量未锁同，净收益精确A/B仍NOT_PROVEN。

| 次序 / ID | R65合批工作与当前层级 | 放行条件 |
|---|---|---|
| 1 / E2、C3 | 同一serializer增加16KiB有界FILE输出；Lua沿原临时文件/提交机制；定向主机与独立审查通过，同进程联调进行中 | 字节与旧reader相容、afterSave/close成对、任何失败不提交；ARM和真机保存重载/峰值/停顿待验 |
| 2 / D1 | 普通内存观测从50ms改250ms；大请求/失败/阶段保持即时，Lua allocator逐次计量与新鲜准入不变 | 实际路由测试通过；记录普通/forced计数与采样峰值边界，真实观测开销下降待验 |
| 3 / S2 | 正常无timer接待员由接待台tick服务；正常成功路径和原生往返已验证，正式callback引用绑定及严格归属资格施工中 | 精确R62日志、唯一实体/医院/接待台关系、实际正式callback身份；仅改ticks，旧副本另名重载后未来服务一致 |
| 4 / 合批验收 | 子流程GPT-6 Astra medium，主线程集成/裁定；独立R65源码现场，R64装机现场保留 | 真实generated Lua+native联调、全部相关回归、完整装配、ARM、中文Git、差量及一次自动设备验收 |

R65之后继续B3温度逐格发布与连续数据、D2/D3模拟/呈现/GPU同步、C类窗口文字与工作集、启动与容量。温度在R64样本累计6.015秒，内存观测2.285秒；World总27.684秒包含子阶段，不能相加。所有优化保留原规则/速度、640×480及当前交互。最终仍须常态中文/语音/音乐下的30FPS+、忙医院/大关/换关/保存连续性、HOME/合盖、长时与正式交付。

## R64 实现记录：采样、员工热点与图片工作集

本批以9226719文档快照和8dace8f装机证据为起点。施工、测试、读回采用GPT-6 Astra medium子流程；主线程整合判断。按用户最新要求，阶段成果做详细中文本地Git提交，暂不推送GitHub。现场与原始回执保存在 `work/iterations/20260909-r64-hotpath-working-set/`，复用上一批脚本和缓存。

| ID | 本批实现 | 校验重点与未完成项 |
|---|---|---|
| D1 | native/Lua共用微秒采样起止，关窗后输出和写快照，截止时间上取整 | 慢日志/500ms写入、错序、失败与亚毫秒边界回归；250ms门槛保留。R64的60秒样本有效，净收益和30FPS另验 |
| B2 | 速度判断合并fatigue/gbv读取，休息分支仅复用未跨可变操作的房间 | 52,000逐步对照和虚派发/RNG回归。极疲劳短路分支可增加guard成本，真实医院净收益待测 |
| C0/E3 | 最近3张改为1.5MiB源字节预算/最多8项LRU；save及安全低内存点清暖集 | 四窗口五图源字节1,321,670，冷加载与活窗口所有权分开。该预算不代表全部原生/GPU内存；GPU图集仍3MiB |
| S2 | 拒绝的旧档副本输出有界逐对象计时器、行动及归属审计 | 索引15为World实体索引。恢复资格未放宽，原档不改；新证据读回后再决定合法恢复路径 |
| 读回 | 精确elapsed_us优先、无效/半截字段不回退；原8项与7个新反例验证 | 原R63两份分析JSON重算保持相同，旧性能结果继续NOT_PROVEN |

本批35项定向回归、实际303 Lua装配与ARM通过；8文件/3,077,829字节差量及自动设备run已完成，详见页首。性能样本有效约15.4FPS；性能净收益与30FPS目标仍未通过。原版速度、640×480布局、人物模拟和存档格式保持。后续B3/D1观测成本、E2有界存档writer、D2/D3调度与GPU、容量/长时队列全部保留。

**历史装机与自动复跑（R63，09-09）**：产品源码为 `8dace8fbb8f1701aaa87fd0d2e9a5bc874693b85`，程序 SHA-256 `60625d561bcd6a30176b4ff7af3bc4183fefdb225d8cfa54ae6c13c38014f753`。自动短测PASS；扩建压力约405秒、21次窗口循环、2次私有保存重载后遇到强制暂停窗口，自动回返，完整22分钟仍为NOT_PROVEN。原25份存档、配置和热键27项fresh SHA不变。[历史脱敏机器摘要](evidence/r63-runner-rerun-20260909.json)。当前R64/R65以页首为准。

**前一批装机记录（09-09 04:32 +08:00）**：源码 `2135006cee431d773cf05e5077c1db1992943e46`，程序 SHA-256 `8d11f0fec0b955bbc75f62b19da406acd44c85852ef7153cd611ce145ed16eb6`。本地完整套件247 PASS/1预期SKIP，ARM与[GitHub十项常规检查](https://github.com/zzliu-coder/corsixh3ds/actions/runs/34273914012)PASS，17文件4,007,011字节差量逐份读回校验通过；809个既有内容文件复用，23份原存档文件与配置保留。[机器回执](evidence/r63-install-receipt.json)。此CI只证明对应旧提交。

当前可销项包含**适配器电脑回归、构建、安装、自动短测回返和健康副本保存重载**。旧档恢复未来行为、真实速率达标、性能净收益、30FPS与大关长时仍待验收。遇到需玩家决定的事件停止样本，保留已完成证据。

## 2026-09-09：中断复跑与 runner 接入

本节为R63历史执行面板，当前状态以页首R64/R65为准。用户要求施工、测试、读回使用 GPT-6 medium 子流程，主线程负责诊断、证据取舍、主计划与调度。优先脚本和增量验证；已成立的同身份证据复用，发生代码或场景变化时只补受影响项。

### 已读回的真实状态

- 中断现场读回时程序为2135006；随后已成套安装754b25c的runner适配，8文件3,121,574字节读回一致。程序SHA为 `946e083064ccd5dff33cbd2dfd1922c3aa3a5cd142f3bce0dc7010ae20f35890`。原Slot1 SHA保持 `d1c6502287adbdf0e64a0ea559843452b23318e83182b48d0673b5e2ad520c32`。
- 四个60秒左右呈现样本均闭合且eligible=1；英文/关音乐、中文/关音乐、中文/开音乐、扩建医院中文/开音乐分别约16.46、15.90、16.72、13.15Hz。这些为呈现API帧间隔观测，正常模拟完成量及恢复仍需独立证据。样本各一次、工作量未完全一致，不推导语言或音乐零成本。
- 四样本之后的窗口/保存压力阶段约15秒即 `ABORT-user-or-lifecycle`，程序随后正常退出；未取得压力阶段COMPLETE。本次用户已说明主动中断，软件标记没有细分具体按键/生命周期来源。
- 当前runner 0.2.0通过随机nonce在线挑战，Old3DS/Rosalina与已验launcher SHA一致，无active/ready/dispatch.lock。它自己的24项真机自测已通过；CorsixTH适配与无人值守回返本节单独验收。
- 原始capture、运行日志及中断标记保持。重跑使用新run ID和私有保存目录，不覆盖旧结果或用户存档；普通游戏入口保持。
- 自动短测 `20260909-111911-4d374adcf4f7` 已正常返回新launcher boot：协议身份和回返PASS；20.025秒完成327次呈现、978次World、326次游戏小时、7310次实体更新；一次私有保存重载断言PASS。第二次进度文件发布失败，整轮FAIL，压力尚未完成。短采样不用于性能验收。
- 失败后原25份存档、配置、热键27项fresh SHA保持一致。准确底层errno未证明：调用方可能在rename前读取errno；本地SDK具备覆盖目标分支。另独立复现主机收集器临时名与远端`.tmp`证据冲突，故本地缺tmp不证明设备缺tmp。两处按成功路径修复并保留失败原件。
- 修后8dace8f的19项定向回归、实际303份Lua装配、ARM构建PASS；3文件2,930,147字节差量读回PASS。新短测 `20260909-114500-2e955595916a` 整轮PASS：20.013秒、320帧、957次World、319次游戏小时、7156次实体更新，一次私有保存重载及两份编号快照均成功；短测不作为性能验收。
- 扩建run `20260909-115104-ac591da14b92` 的身份/回返PASS；中文、所选中文语音库和音乐开启。60.058秒记录909帧、2727次World、909次游戏小时、29317次实体更新。随后约405.040秒完成21次窗口循环和2次私有保存重载，遇到 `NEEDS_INPUT` 停止，完整22分钟NOT_PROVEN。三份编号快照完整，原27项fresh SHA不变。
- 强制暂停发生在 `2-01-01T00` 附近，日志观察到 `UIAnnualReport`，该窗口源码要求暂停，年度报告是强支持的解释；缺少触发瞬间完整窗口列表。队列未代答、重投或清锁。
- R62副本资格检查实际拒绝 `staff has no resumable timer: 15`；15为 `world.entities` 索引。完整条件未通过，未强行解冻，旧档恢复重载和未来行为仍NOT_PROVEN。上述2次保存重载属于健康基准世界。
- 本次正式性能观测NOT_PROVEN：Lua时长60,058,000µs，原生闭合60,507,141µs，相差449,141µs，超过既有250ms容差。源码在取Lua结束时间后先打印/持久化快照，再关闭原生样本。后续调整边界顺序，保持验收门槛；约15.1Hz只作诊断值。日志文件未写失败/截断，慢事件环有丢弃，完整慢事件历史未证明。

### 本批工单与销项条件

| ID | 子流程/工作 | 当前状态 | 销项证据 |
|---|---|---|---|
| R01 | 读回：身份、四样本、中断、原档、runner在线核对 | 取证完成；明细独立复核 | 原始日志+SHA+nonce回执，保留未完成结论 |
| R02 | 施工：显式runner参数、独立目录/存档、结果持久化、正常退出回返 | 8dace8f已安装；有界不可变checkpoint、精确IO失败留证通过 | 新短测2份/扩建3份合法快照；原27项不变。主机收集器修复2428a12仅本地提交 |
| R03 | 测试：受影响主机回归、实际生成模块、原生构建/身份 | 8dace8f的19项定向回归、303 Lua实际装配与ARM PASS；独立反例与离线结果复核PASS | 复用2135006未变模块的既有结果；本次完整里程碑套件不重跑，不沿用旧CI证明新代码 |
| R04 | 真机：短中文常态冒烟→结果读回→回返runner | 新ID短测PASS；先前FAIL原件保留 | 候选/输入/配置匹配，新return boot，私有保存重载与模拟完成量，原档保护 |
| R05 | 真机：扩建中文+中文语音+音乐，补未完成窗口/存读档压力 | NOT_PROVEN/NEEDS_INPUT；405秒、21循环、2次保存重载PASS，完整22分钟未完成 | 先修采样边界、记录强制窗口类别；后续拆分可自动的容量回归与需决策的年度/事件验收 |
| R06 | 主线程：分析、更新五框架排序、中文Git提交/推送 | 本次结果及下一批顺序已登记，产品8dace8f已推送；报告提交单列 | 实现/主机/装机/真机正确性/性能收益分别登记，30FPS目标仍待验证 |

本批实现说明见 [R63 runner 适配与回归边界](R63_RUNNER_ADAPTER.md)。25份真实存档及配置、热键、身份、三个测试输入共33项已读回保护；长测后33项均复用同SHA本地副本。安装与每轮run分别记身份，下一候选以已安装8dace8f作为增量基线。报告后续提交只改变文档，装机源码身份保持。

独立反例曾拦下清理失败前提前记录COMPLETE，现改为非终态WORKLOAD-END；最终成功由result.kv及launcher新启动回执共同确定。生成导出测试曾因新增文件未进入Git索引而缺源，登记新增源后同组17项通过。实际ARM构建发现strict C++17下fileno声明缺失，按现有3DS宏配置补齐并保留fsync；全链接与636个移植符号单帧栈检查通过。失败阶段证据保留，不作为设备故障。

### 测试去重规则

1. 本次已完成四媒体样本保留，不因后面中断全部作废。日常回归默认中文+所选中文语音+音乐；英文关音乐和中文关音乐长采样不再每轮重复。
2. 修改字体排版、缓存、语言/音频切换时补相关中英文窗口与切换回归；修改混音/流式读取时再做媒体开关配对。历史单样本不足以承诺零开销。
3. 当前先证明一次短自动回返，再补未完成的22分钟压力；后续例行回归可采用四窗口各冷/热一次及私有保存重载。缩短的回归不能替代长期稳定性验收。
4. 原有工具链、静态资源和同提交电脑验证按哈希/改动范围复用。每轮上传候选、改动运行脚本及小输入；大型素材沿用已验证部署，明确记录本轮未全量重验。
5. 与性能采样无关的runner自身24项全套不每次重跑。游戏端新增桥接、成功/取消/失败及连续新ID回返必须独立验证。

### 本轮读回后，五框架的优先级

继续先完成S2/S3真实行为和保存连续性。常态中文样本中，World约25.6秒/60秒，包含Entity约16.6秒和温度约5.8秒；这些是嵌套计时，不相加。扩建样本内存观测自身约4.75秒/60秒，值得做有界观测开销对照。GPU上传在这些稳定镜头样本中占比较小，冷窗口与图集竞争仍待压力数据。

据此，下一批优先调查B2员工/世界热点、B3温度及D1观测开销，C1/C2窗口文字/图集按压力结果排序；E2有界保存继续保留。当前不据单次采样同时打开所有算法/调度实验，也不宣称已完成30FPS目标。

### 本次压力读回后：下一批的最小施工顺序

以下为待施工，沿用原ID；每个改变独立提交及主机等价对照，汇成一次候选上机。无需再为未改的中英文/音乐组合重复矩阵。

| 顺序 / 原ID | 已有证据和下一步 | 销项门槛 |
|---|---|---|
| 1 / D1、S3 | 先关闭采样边界，再打印和写SD快照；开始/结束共用单调时钟。NEEDS_INPUT记录窗口类和保存点，年度报告/经营选择单独列验收 | 注入500ms快照延迟不改变已闭合样本；保持250ms门槛与缺失/失败拒绝；不自动回答经营事件 |
| 2 / S2 | 专用旧档索引15缺可恢复timer；补逐对象timer/action/归属诊断，检查完整对象图中可证的正常调度来源 | 原档不写；条件不明继续拒绝；获资格后另名保存重开并观察未来工作，不能全体解冻 |
| 3 / B2、A1 | 扩建样本World约27.28秒，其中实体约16.71秒；员工采样的速度/属性段较高。先合并同次updateSpeed内疲劳与gbv读取，再评估checkIfNeedRest内重复getRoom | 实际方法对照疲劳阈值、培训/职业/作弊、休息队列与逐次属性；检查子类重写；不改变RNG、夹取或持久化字段 |
| 4 / C1/C2、E3 | 压力序列重复打开Pol01V/Rep01V/Res01V时raw-image仍约1.21–1.26秒。当前弱查找+仅3张近期源图，与4窗口/附属面板循环不匹配；先定位源图淘汰，再分开看GPU图集重传 | 有界字节工作集及压力回收；活窗口所有权保留；冷/热窗口SD读取数、解码/上传与长停顿下降。读取与raw总耗时嵌套，不相加 |
| 5 / B3、D1 | 温度约5.91秒，内存观测约2.22秒/该60秒诊断段。整数/邻接缓存已经存在；查温度连续布局与逐格发布成本，减少高频重复堆观测 | 温度逐步数值/存档一致；真实内存准入和分配失败仍即时检测；采样成本与峰值覆盖分账，按实际净收益保留 |
| 6 / E2→D2/D3 | 私有保存/重载正确但仍长停顿；有界writer按既定方案推进。降低单次世界回调和绘制成本后，再调补算/呈现同步 | 保持旧档、格式及失败注入；同时检查World完成量、18ms时钟债务、输入积压和呈现率，继续目标30FPS+ |

新扩建样本约15.1次呈现/秒只作定位；同60.058秒的World完成2727次，对照18ms理论约3337次，提示实时推进仍有欠账。采样结束区间偏差尚未修正，精确性能验收保持NOT_PROVEN。小幅Lua查询优化只能贡献部分收益，后续还需按关键路径处理绘制、窗口、温度和调度。

## 前一批执行面板（R63 外审增补，已安装）

本面板覆盖本文后面第一批的历史状态。外部重审已交付，固定审查对象为 `ecc6b6f`；本提交包含随后修复，外审没有验收这些新代码。原始报告 SHA-256 为 `cf12b144ec9677657a3fa3357f00f75dfa114b0cd3168be41a74073ea390a53a`。

| 顺序 / ID | 本轮交付范围 | 电脑侧结果 | 真机销项条件 |
|---|---|---|---|
| 1 / S1 | Staff/BottomPanel 正确取得平台接口，严格模块执行 | 相关回归 PASS | 跨多轮员工更新，无 TH3DS 错误，原有员工与新患者持续工作 |
| 2 / C1 | 独立 request_width 键；保留双候选、128 槽与原字节预算 | 有限/无限请求互换碰撞的真实 FreeType 排版与像素 PASS | 中英文窗口不串排版；采样 CPU 重建、GPU 重传和长帧 |
| 3 / S4、D1 | 内层栈展开前保留错误；分别数派发/完成/失败、World/游戏小时/Entity 成功完成量 | 嵌套/非字符串异常、模拟途中失败和零完成量拒绝 PASS | 错误数为零；FPS 与日期/工作推进同时报告 |
| 4 / B2 前置 | 员工采样按每个更新轮次轮换位置，不使用游戏 RNG；profile-off.txt 可关闭详细 CPU 采样 | 16员工×64轮均覆盖；关闭后不取相位时钟 PASS | 分段成本可信；关闭前后性能差异尚待 A/B |
| 5 / S2 | 原档保留；专用副本只按完整错误历史、员工归属/生命周期、行动与计时资格恢复；全部检查后才改 ticks | 负例拒绝、实际 errorHandler/Entity 路径和原生序列化回归 | 实际 Slot1 资格、恢复人数及未来行动仍 NOT_PROVEN |
| 6 / S2 保存隔离 | 恢复会话默认/三槽/快捷/退出均使用 R63-Recovered- 前缀；原 Slot1 显式覆盖仍需原确认 | 随保存与 UI 回归验证 | 另名保存→退出→重载状态连续，原档哈希不变 |
| 7 / A1、B1/E1 | 保留空垃圾查询跳过、整数编码栈缓冲；其他已有优化不回退 | 查询一致；整数编码微基准 7047 次临时分配→0，字节相同 | 真实医院中的查询、保存耗时和峰值有净收益 |
| 8 / D1 媒体 | 英文/中文语音库明确切换并核实装载；读取 mixer 实际音乐播放/暂停状态 | 媒体事务及基准编排验证 | 中文音乐常态负载；实际播报音频听感另验，装载不等于已播报 |
| 9 / 发布 | Linux Lua 开发依赖/编译器运行库、测试路径、GPU 严格警告修复 | 完整套件、Linux CI、ARM 构建以本提交回执为准 | 差量读回一致后允许开机测试，设备验收仍 NOT_PROVEN |

装配检查另外发现新 state_health.lua 未进入此前手工复制名单。已改为按唯一 lua/3ds 源目录枚举运行模块，集成清单同时记录；回归逐文件核对生成字节，并从实际生成目录 require 全部模块。包检查拦截发生在上传之前，原设备未受影响。

**旧档恢复与健康性能分账。** R62留存运行日志有回调不存在、空行动队列警告；运行日志不能直接代替Slot1内的对象图事实。Slot1保存内容能找到已知员工错误和恢复记录，程序还会核对副本自己的game_log及逐对象资格。资格不能证明时拒绝恢复并打印原因，独立健康基准继续且提示旧档需审计。健康基准完成不能销掉旧档恢复。

本批基准是同起点的固定现实时间观测，增加实际完成量和媒体装载/播放记录。`exact_state_ab=NOT_PROVEN` 明确提示：30秒现实预热后的医院状态可能不同，不能将它写成相同工作量的精确 A/B。恢复指纹覆盖部分关键状态，真实未来行为另验。

### 后续按这条队列推进

1. **S2/S3 闭环**：读本候选日志；核对旧档资格、健康员工/患者工作和保存重载。安全拒绝的旧档保留，逐对象诊断，禁止全体重建/解冻。
2. **B2/A1 世界更新热点**：按员工基类、休息、属性、垃圾、速度分段成本选最热入口；补人数1/15/17/32及插入删除的采样对照、早退/职业分布。候选算法须保持行为、随机次序和计时规则。
3. **C1/C2/A2 绘制与窗口**：优先测“仅排版却生成像素”、冷/热窗口重复文字、CPU缓存与GPU图集相互淘汰；按实际重建/上传代价决定文字两阶段生成、遮挡裁剪或页分类。各自独立 A/B，不能一起开后猜收益。
4. **E2 保存与启动**：有界写入接现有 .tmp/原子提交，减少完整 C++/Lua 输出副本；覆盖短写、磁盘满、取消、序列化/提交失败。再按加载阶段选择缓存预处理，继续复用素材和工具链。
5. **D2/D3 调度和GPU同步**：先降低重回调成本，再研究呈现复用、补算预算与精确刷新；保留队列栅栏及花屏回归。累计完成量、债务、长帧与输入滞留共同改善才保留。
6. **容量和交付**：中文+所选语音+音乐开启，忙医院/大关、换关、重复存读档、HOME/合盖、长时运行。正常速度与最高原版速度分别评价。达到30FPS轻负载仍需容量和稳定验收。

每个 ID 只有在对应层级证据到齐后销项。电脑正确性 PASS、差量上传 PASS、真机正确性 PASS、性能收益 PASS 分开登记；禁止用“已实现”代替以上四种结果。

## 0. 当前事故与证据边界

- 设备核对版本：R62，源码 `cd553e3ccea12b1fca47b6d30cc23935e10902ac`。
- 设备程序 SHA-256：`b6515fdf8cb348084e65554119d23fec51db7fa23d21bf8a01fc5586e8893de4`，FTP 流式重算一致。
- 两次游戏日志均记录 `entities/humanoids/staff.lua:127: use of undeclared variable 'TH3DS'`。R62 每十六次员工更新的采样访问了其他模块的局部变量名；真实上游开启严格全局检查。
- 上游 `App:errorHandler` 的恢复操作会设置出错实体 `ticks=false`；此字段进入存档。它解释了恢复后员工停止更新、后来生成的人物仍能更新的路径。具体存档受影响人数要由读取状态确认，不能从截图猜测。
- 同类遗漏还存在于大窗口预加载：`UIBottomPanel:addDialog`。一起修正平台绑定，并覆盖完整生成模块的严格环境执行。
- R62 自动 benchmark 为 **FAIL**。异常后少算人物得到的帧率无效，不能与健康的 R61 比较。R61 历史正常基准约 11.7～11.9 FPS；它只能作为历史参考。
- 本次 Slot1 360,217 字节，SHA-256 `d1c6502287adbdf0e64a0ea559843452b23318e83182b48d0673b5e2ad520c32`。原档、备份及两个运行日志均已本地保留。公开仓库只发布代码、日志摘录、哈希和结论，不发布游戏素材或私人存档。

## 1. 成功路径与不变量

启动并核对程序 → 载入已确认健康的医院副本 → 所有应更新实体按上游规则更新 → 原速率推进日期与行为 → 正确绘制与触控 → 保存提交成功 → 重开后员工、患者、房间、地块和计时状态相符。

每一项性能工作都不得以停掉实体、降低场外模拟频率、改速度档或跳过必要存档内容获得收益。保持当前 640×480 游戏画布、上下屏分工、摇杆边缘滚图、触控鼠标、清晰／广角切换。

## 2. 先行稳定性门槛 S

| ID | 工作与代码边界 | 验证和完成条件 | 状态 |
|---|---|---|---|
| S1 | 修复 Staff 与 BottomPanel 平台绑定；审计所有生成 Lua 模块的原生接口引用 | 完整模块加载于严格环境；至少跨越 16 次采样边界；平台存在／缺失均能执行；窗口成功和资源失败路径正确 | 主机PASS；健康真机fixture跨员工更新/21窗口循环无原绑定错误；大关/长期仍待验 |
| S2 | 确认 Slot1 停更实体；修复仅限有 R62 精确错误证据的副本 | 原档零修改；只恢复已识别员工的更新标记；不重建人物、不改行动队列和属性；再次存读档状态一致 | 主机PASS；真实副本索引15缺timer被拒绝；旧档未来恢复NOT_PROVEN |
| S3 | benchmark 纳入实体健康与 timer 错误检查 | 输入已有停更员工、运行中异常或计时器注销立即使样本无效；不输出性能 PASS；不自动回答突发事件 | 主机故障注入PASS；R64完成量/健康保存重载及有效采样边界通过；完整长测未通过 |
| S4 | 补错误日志保真 | 原异常文本可关联采样和阶段；日志有界；监控失败不得通过改变模拟状态伪装性能改善 | 已实现、真实 Lua 嵌套/非字符串错误 PASS；设备日志待验 |

S1–S3 未通过时，不评估 R62 帧率收益，不部署大型调度或图集实验。

## 3. 五框架总清单

状态约定：`已实现` 表示已有代码与相关主机验证；设备收益必须另外给出。`本批` 为 R63 合批范围；`候选` 需通过明确的进入条件。表中尚未勾选的项目不会在交付时写成已完成。

### A. 少做工作：算法、查询、变化驱动

| ID | 真实入口与改法 | 等价性／失效条件 | 观测与放行 |
|---|---|---|---|
| A0 | 已有稀疏 EntityMap、直接邻域、垃圾 visitor、地图视野裁剪 | 保留 x/y/对象顺序与房间归属 | 已实现，R62 收益重测 |
| A1 | 本批检查并缩短员工垃圾搜索的稳定／空结果路径 | 垃圾增加、移除、移动、借出可变列表及读档必须可靠失效；无法证明时使用原查询 | 查询次数、扫格数、原生房间调用与结果逐步对照 |
| A2 | 完全不透明窗口覆盖医院时减少底层绘制 | 保留窗口透明、绘制顺序、震动随机调用和命中检测副作用 | 候选；先证明完整覆盖，比较冷／热窗口 CPU |
| A3 | 稳定地板／阴影命令复用，暂停画布复用 | 镜头、地板、墙阴影、动画、UI 变化分别失效；不重排墙和人物 | 候选；缓存维护成本低于省下的命令工作才保留 |

### B. 必要工作更便宜：热循环、布局、批处理

| ID | 真实入口与改法 | 等价性／边界 | 观测与放行 |
|---|---|---|---|
| B0 | 已有温度整数／邻接缓存、员工动态文字内容复用、输入表复用及热点 O2 | 保留浮点／整数截断、属性夹取和原调用顺序 | 已实现；修复观测后复测分项 |
| B1 | 本批将 `persist_lua.h::write_uint` 的逐值临时 vector 改为固定最多 10 字节栈缓冲 | 同一类型和值输出逐字节相等；无新堆申请；32/64 位边界与负数 zigzag 对照 | 主机原生格式测试、ARM 编译、writer 时间 |
| B2 | 员工基类、休息、属性、垃圾、速度五段定位后只改最热的真实入口 | 不合并有夹取的属性变化；不改变疲劳、快乐、薪水、随机次序 | 候选；细分采样含调用数、比例和早退；健康医院对照 |
| B3 | 温度局部结构失效、Lua／原生批处理及真实依赖编译对照 | 墙、房间、暖气和换关覆盖；缓存权威性不变 | 候选；原始实现逐步数值对照；不默认重写 Lua VM 或寻路 |

### C. 少持有、少搬运：所有权、缓存与工作集

| ID | 真实入口与改法 | 空间／生命周期边界 | 观测与放行 |
|---|---|---|---|
| C0 | 背景8位源、128px分块、弱查找＋1.5MiB/最多8项暖集、3MiB图集、2MiB文字预算 | 窗口拥有活图；排队GPU命令完成后才能淘汰；音乐流式、音效按需 | R64已安装；11轮窗口有界压力通过；完整命中/重传计数及净收益待验 |
| C1 | 本批评估文字缓存双候选槽，减少哈希冲突重建 | 总槽数和字节预算不增加；完整键含文本、宽度、对齐、行数；释放唯一 | 实现需碰撞／命中／驱逐计数及文字像素回归 |
| C2 | 图集按热小图／大背景／文字降低整页相互挤占 | 维持既有 3MiB 预算、弹性借用；不能因分类产生空置饥饿；严格队列栅栏 | 候选；先测 R62 修复后窗口序列重传，收益成立才改 |
| C3 | 字形覆盖图共享、调色板上传格式、存档输出中间副本 | 缓存大小有上限；前景色、阴影、中文尺寸不变；临时／驻留分账 | 候选；以解码时间、峰值和上传量决定，磁盘压缩率次要 |

### D. 合理安排时间：模拟、输入、呈现和同步

| ID | 真实入口与改法 | 行为边界 | 观测与放行 |
|---|---|---|---|
| D0 | 已有输入采集、合并、有界补算、双屏共用 GPU 画布 | 游戏速度档完全沿用上游；18ms 基础时钟及债务单独计量 | 已实现 |
| D1 | 本批把模拟健康作为性能采样前提，修复采样接入与错误传播 | 员工停止更新、timer 注销、游戏错误均使样本无效；采样不持有游戏对象 | 主机故障注入；固定 Normal 日期推进和有效画面 FPS 同验 |
| D2 | 先降低单个世界更新成本，再研究预算和纯呈现复用 | 一次重回调不能被预算检查中途打断；不靠扩大补算数量挤占输入 | 候选；长帧、输入滞留、模拟债务共同改善 |
| D3 | GPU 队列分段、刷新范围、同步等待 | 顶点、索引、图集、输出全部覆盖；保留已修好的花屏与资源生命周期边界 | 候选；实际阶段显著才做精确刷新实验，避免盲目绕过同步 |

### E. 冷路径与长停顿：预处理、加载、保存

| ID | 真实入口与改法 | 正确性／回退 | 观测与放行 |
|---|---|---|---|
| E0 | 已有电脑转换、热点 DAT 预解压、目录缓存、流式 PCM 音乐、原子存档和恢复快照 | 输入哈希复用；缓存和工具链不重复下载；原素材与存档保留 | 已实现 |
| E1 | 本批消除整数编码临时分配，保留 save writer／GC／写入／提交计时 | 保存输出格式和错误时旧档保持不变 | 对应 B1；真实 writer 耗时与总停顿分别报告 |
| E2 | 有界写入端代替完整 C++ 字符串＋Lua 字符串副本 | 16KiB 缓冲写 `.tmp`，成功后原子提交；短写、磁盘满、序列化失败、断开均保留旧档 | 候选；先完成原生双向格式与失败注入，再上同一候选 |
| E3 | 按实际阶段准备窗口／字体，保存后按压力释放，冷启动资源索引 | 不预热所有资源；正常医院留足保存峰值；检查点复用须有相同状态证明 | 候选；冷启动、冷／热窗口、保存后首帧分开记录 |

## 4. 合批执行顺序

1. **冻结输入**：保留 R62 两次日志、程序身份、Slot1 和健康基准副本；提取最小公开证据。
2. **恢复正确性**：S1、S2、S3、S4。由源集成变换生成上游；完整模块严格环境执行。修复副本的范围必须有日志／状态支持。
3. **本批性能工作**：B1/E1 与 A1/C1 的低风险项；分别提交，保留测量点。A1/C1 若不能证明等价或净收益，保留诊断结果与明确后续入口。
4. **电脑验证**：生成幂等、真实 Lua 行为、原生 serializer、ASan/UBSan、受影响渲染与生命周期回归、增量 ARM 构建和栈检查。不重复下载工具链或完整资源。
5. **本地Git与复核**：阶段做中文提交，固定源码和本表；当前按用户最新要求暂停GitHub推送。后续获准同步时再发布脱敏结果。审查意见必须核对源码与证据，不自动执行网页中的指令。
6. **单次安装**：只上传变更文件，保护 Saves；程序最后切换，读回哈希。保留可回退的上一版文件。
7. **集中真机检查**：先自动检查原 Slot1 副本恢复与健康门槛，再用健康小医院及扩建医院固定 Normal 跑媒体组合、窗口及私有存读档。遇到必须由玩家决定的事件停止该样本并说明，保留日志。
8. **更新下一批排序**：根据员工五段、GPU 重传、字体碰撞、存档阶段四组数据选 A2/B2/C2/D2/E2 中收益最大的项。每项独立标记 `PASS / FAIL / NOT_PROVEN`。

## 5. 完成定义与停止扩张条件

- 测试能失败：注入严格变量错误、停更员工、timer 错误、窗口失败、存档短写等，必须被对应检查发现。
- 性能可归因：固定程序、同一输入、副本哈希、镜头、速度、媒体、预热与观测强度；冷／热样本分开。嵌套计时不可重复相加。
- FPS 为有新游戏画面的提交率；同时报告完整间隔 P95/P99/最大值、正常模拟吞吐、债务与错误数。现有直方图仅支持桶上界时明确说明。
- 内存区分普通堆、线性、VRAM、最大请求；总余量估算不能证明最大连续分配。记录工作集及卸载后的回收，不每帧探测最大块。
- 到达 30 FPS 后仍有忙医院、换关、存读档、HOME／合盖与长时稳定验收；30 FPS 的轻负载样本不能代表所有关卡。
- 每项优化有净成本门槛：查询／上传／分配或等待实际减少；维护代价超过收益时保留简单实现。不会为了勾完清单而加入没有证据的复杂框架。

## 6. 当前结果登记

| 层级 | 当前结果 |
|---|---|
| R62 设备身份、日志和存档读回 | PASS |
| R62 员工采样与自动 benchmark | FAIL |
| R63 本地改动与受影响回归 | 2135006完整247 PASS/1预期SKIP、Linux十项；8dace8f追加19项定向、303 Lua实际装配与ARM PASS，按改动范围复用 |
| R63 当前差量上传 | 8dace8f：PASS，3文件/2,930,147bytes；原27项fresh SHA不变，上一版回退文件保留 |
| R63 自动回返与健康副本存读档 | 短测整轮PASS；扩建中2次私有保存重载PASS，回返/快照身份PASS |
| R63 真机旧档恢复、正式性能与长时容量 | NOT_PROVEN：旧档缺timer资格；性能区间相差449141µs；22分钟序列在405秒NEEDS_INPUT停止 |
| ChatGPT 外部审查 | 已交付固定旧提交独立重审；新修复按本页执行面板验证，无需继续等待旧定时任务 |

### R63 第一批历史记录（本轮增补以页首执行面板为准）

- S1：完整生成的 Staff/BottomPanel 在真实上游 strict.lua 下执行通过；覆盖 128 次员工更新、原生平台存在／缺失、窗口准备成功／失败。
- S2：具备精确错误及匹配恢复记录时，只恢复专用副本的 Staff 更新标记。患者、未知错误、未匹配记录或数量不符均拒绝；原始 Slot1 不覆盖。真实 Slot1 的人数与重载行为仍待设备确认。
- S3/D1：采样前和每五秒检查人员状态，逐帧检查错误计数和 timer；存读档比较更新标记、坐标、计时器及动作名称／标记。失败后暂停并停止样本，不输出 COMPLETE。
- A1：可信的全局垃圾数量为零时，省去员工周边查询；新增、移除、移动、读取存档时维护；借出的可变列表始终走保守查询。派生计数在弱键表中，排除于存档图之外。
- B1/E1：整数编码使用最大 10 字节栈空间。8/16/32/64 位边界、随机输入和有符号映射逐字节对照通过，测试输入的 7,047 次临时堆分配降为零。
- C1：每字体仍为 128 个槽、全局仍为 2MiB 保留预算；为双候选替换增加每字体约 1KiB 固定时间戳元数据及全局计数。200 次碰撞文字交替绘制为 2 次重建／198 次命中，重复像素一致；字体释放与超预算瞬态也回归。
- 第一轮受影响回归 18 项通过。后续完整套件、ARM 构建及设备效果单独登记；这组结果不构成 30 FPS 或真机恢复通过。
- 外部审查对话：[审查移植仓库](https://chatgpt.com/c/6aa034ee-7a8c-83e8-8955-776809447900)。固定审查提交仍为 `ecc6b6f`，后续本地施工单独提交，避免审查对象漂移。
